# Decisions Log

- 2026-07-25 — T1: content data (enemy stats, moves, items, gear, drop rates, thresholds) lives in seeded Postgres tables served to the client as a versioned bundle, rather than as client-side JSON. Reason: single transcription point for GDD tables, assertable against the fixtures by a real test, FK integrity for player rows. The client-side battle engine still works offline off the cached bundle.
- 2026-07-25 — T1: `xp_curve` is seeded as 60 precomputed rows rather than evaluating `round(100 × L^1.05)` at runtime. Reason: .NET banker's rounding and JS `Math.round` disagree at exact halves, and client/server must never disagree about a level-up.
- 2026-07-25 — T1: two ID families used in the schema are not yet in `traverser-data-manifest.md` — zone-gate keys (`gate_cyclops`, `gate_cerberus`, `gate_fenrir`, `gate_jormungandr`, `gate_griffin`, `gate_cacus`) and the expanded gear keys implied by the manifest's `gear_{slot}_{tier}` pattern (12 concrete keys). Add both to the manifest before M0 seeding.
- 2026-07-25 — T1: `gear_move.source_gear_id` dropped in favour of a single direction (`gear_def.grants_move_id`), avoiding a circular FK. The manifest's reverse mapping is a lookup, not stored.
- 2026-07-25 — housekeeping: this file was UTF-16LE with mangled `×`/`—` characters; rewritten as UTF-8. Content unchanged.
- 2026-07-25 — T2: the server is the progression authority (XP, level curve, Leagues, gate state, encounter RNG); the client keeps battle resolution only. Reason: progression is cumulative and irreversible, so the level curve is implemented once, next to `xp_curve`. Does not change the sanctioned client-side-battle trim.
- 2026-07-25 — T2: sync issues encounter *grants* rather than delivering battles — enemy and zone resolved server-side, counted against the 5/day cap at issue time, spent by the client whenever (online or not). Reason: reconciles server-owned encounter RNG with fully-offline play, since the API is off between sessions by design.
- 2026-07-25 — T2: `client_delta_id` is a UUIDv7 minted when the delta is created on-device and never regenerated on retry. Explicitly **not** a content hash or `(date, source, steps)` tuple — two legitimately identical deltas would collide and one would be silently dropped, losing real steps.
- 2026-07-25 — T2: additive merge (`col = col + delta`) is mandatory for steps, tier minutes, and all XP/lifetime totals, which are monotonically non-decreasing on the server. Last-write-wins is retained deliberately for settings, equipped gear, skill loadout, and `hr_session` tier minutes, where the newest value is the truth.
- 2026-07-25 — T2: guest identity is a device-generated `player_id` plus an opaque long-lived bearer token. Reason: T6 exposes the API over Tailscale, so an unauthenticated write surface isn't acceptable even single-player. Real auth later is additive (`auth_identity`), no endpoint changes.
- 2026-07-25 — T2: multi-device sync (GDD 11 §11) remains open and unsolved. Additive-over-ledger merge means concurrent devices can't corrupt totals, but per-day streak/grace evaluation would need a design pass. Seam noted, not built.
- 2026-07-25 — T3: **GDD 10 deviation.** A birth-year field is added to the Screen 3 character-creation step. Reason: GDD 1 §2.2 requires `HRmax = 220 − age` and the locked eleven-screen onboarding never collects age — a gap between two locked specs. Health Connect exposes no dependable age/DOB record type. Editable later in Settings; changes apply to future reads only and never recompute past days.
- 2026-07-25 — T3: HR sessions are segmented from the heart-rate sample timeline (GDD 11 §8.1's 10-minute-drop rule), **not** from Health Connect `ExerciseSession` records. Reason: the GDD defines a session in terms of elevated HR, not a workout the player remembered to start; anchoring to exercise sessions would award nothing for a brisk unlogged walk. Cost is the loss of a provider-issued session ID.
- 2026-07-25 — T3: consequently `hr_session.external_session_id` takes tech-01 §7's pre-authorised fallback, encoded as `"hr:{started_at epoch seconds}"` so the existing `unique (player_id, external_session_id)` index enforces the `(player_id, started_at)` key. **No schema change.** A local ledger freezes each session's `started_at` so backfill can't shift the ID.
- 2026-07-25 — T3: tier minutes are derived by 1-minute buckets scored on the mean BPM of the samples in each bucket; minutes with no samples are untiered. Reason: integer minutes fall out of the algorithm rather than being rounded in at the end, the unit matches GDD 1 §2.2's per-minute rates exactly, and every case is fixture-assertable. Spike probe 1 may replace this with sample-interval weighting if real sampling proves too sparse.
- 2026-07-25 — T3: daily steps are read via `aggregateGroupByPeriod` rather than raw `readRecords`, specifically because Health Connect de-duplicates overlapping step contributions from multiple origins (phone + watch) during aggregation. Doing that reconciliation ourselves is out of sanctioned scope, and summing raw records would double-count every day for any player wearing a watch.
- 2026-07-25 — T3: the client mints step and HR-minute deltas as the **difference** over a persisted per-date high-water mark, never as the day's total, because T2 §6.1 merges additively and the read window re-reads days already reported. A downward provider revision mints nothing and does not lower the mark.
- 2026-07-26 — **spike/health-connect run on a Pixel 9 (Android 16), data source `com.fitbit.FitbitMobile`.** Findings below; environment: `react-native-health-connect` 3.5.3, Expo SDK 57, local `expo run:android` dev build. Probe numbering per T3 §12.
- 2026-07-26 — T3 §3 **correction (probe 6): a read without permission throws, it does not return empty.** Verified for both types: `HealthConnectException: java.lang.SecurityException: Caller requires android.permission.health.READ_STEPS/READ_HEART_RATE`. T3 §3's claim that "a revoked read returns an empty result, not an error" is wrong. The read path needs a catch mapping the exception to the banner state — probe 6's stated fallback. The distinction T3 draws between empty-and-unpermissioned vs. empty-and-sedentary is now trivially safe: the former can't produce an empty result at all. *(Tested via denial; revoke-after-grant not separately tested, same native path.)*
- 2026-07-26 — T3 §3 **addition: `initialize()` is per-process and must precede every read**, not just onboarding. Changing permissions in Health Connect settings restarts the app process, after which every call fails with "Health Connect client not initialized". T3 §3 currently says only `getGrantedPermissions` re-runs each foreground; `getSdkStatus` → `initialize` must join it.
- 2026-07-26 — T3 §3 confirmed: **re-requesting after a denial is a silent no-op** — `requestPermission` returns `[]` and no dialog appears. The `openHealthConnectSettings()` deep link is mandatory, exactly as specified.
- 2026-07-26 — T3 §3 **addition: granting Steps also silently grants `StepsCadence`**, a record type never requested and absent from the manifest. `getGrantedPermissions` can therefore return more entries than were asked for; it must be checked by exact `recordType` match, never by array length or index. Partial grant otherwise behaves as specified (probe 7 passes — Steps granted, HeartRate denied, reported independently).
- 2026-07-26 — T3 §5.2 **confirmed unchanged (probe 1).** Fitbit HR sampling is dense: median inter-sample gap **2s** (min 1, max 3), zero gaps over 60s. Whole-minute bucketing is viable; the sample-interval-weighting fallback is **not** needed and fixtures stand.
- 2026-07-26 — T3 §4.2 **confirmed (probe 5).** `aggregateGroupByPeriod` slices on **local** midnight: at UTC-4 a bucket came back `2026-07-26T00:00 → 00:11:42`, not the 20:00 a UTC slice would give. Mechanism: the library parses the filter with `Instant.parse(x).atZone(systemDefault()).toLocalDateTime()`. **Consequence for M1:** the time range filter must be given a **UTC instant string** (`toISOString()`); a local-naive string throws `Text '...' could not be parsed at index 19`. The library does the local conversion itself.
- 2026-07-26 — **new, not anticipated by T3: reads are paginated and must follow `pageToken`.** A 48-hour HeartRate read returned exactly **1000** records — the default `pageSize` cap — with `pageToken` present. Any read assuming one call returns the full window will silently truncate. T4 owns this.
- 2026-07-26 — **new, not anticipated by T3: Fitbit writes HR as ~one record per minute, not one per workout.** Record durations min 25s / median 57s / max 59s, averaging 25.6 samples each. The densest single record is 32 samples over 59 seconds. T3 §6 session identity must stitch hundreds of adjacent records into a session; no single record ever represents one. Reinforces the §5.2 decision to segment from the sample timeline rather than from provider records.
- 2026-07-26 — T3 §6 confirmed (probe 3): `metadata.id` was **stable** across app restarts and a permission revoke/re-grant cycle (same UUID both runs). Diagnostics can rely on it; the ledger still keys on derived session identity per spec.
- 2026-07-26 — **historical depth on this device is ~30 days**, not longer: 197 step records at 30d ago, 0 at 90d, 0 beyond (same shape for HeartRate). Comfortably clears T3 §4.1's 72-hour window, but GDD 11 §3.2's grace lookback must not assume more than ~30 days of recoverable history.
- 2026-07-26 — **T3 §4.2 VERIFIED (probe 4): `aggregateGroupByPeriod` does de-duplicate across origins.** A walk carrying the phone and wearing the watch produced two independent writers — `com.fitbit.FitbitMobile` (348 steps) and the phone's own sensor `com.android.healthconnect.phone.j8803…` (253 steps) — for a raw sum of 601 across 11 records, against an aggregate of **373**. 228 duplicated steps were discarded by Health Connect. Summing raw records would have inflated the day by ~60%. The single-origin fallback is not needed and `aggregateGroupByPeriod` stands as specified. *(Two earlier attempts were inconclusive and are worth remembering as method notes: with the phone plugged in and stationary only one origin appears, and before the watch syncs the two origins' contributions are too small and disjoint to collide.)*
- 2026-07-26 — **the phone writes steps to Health Connect independently of Fitbit**, under `com.android.healthconnect.phone.<hash>`. Multi-origin is the normal steady state on this device, not an edge case, which is what makes §4.2's reliance on aggregation load-bearing rather than defensive.
- 2026-07-26 — **T3 §12 probe 2 (backfill latency) remains UNCHARACTERISED.** Observed only that a read taken minutes after a walk showed 23 steps while the true figure was ~373 — the watch's contribution had not yet reached Health Connect. A manual Fitbit sync was then triggered out of impatience, so **whether automatic sync would have delivered it, and how quickly, was not tested.** The finding that matters is directional and sufficient for M1: a read taken shortly after activity can understate the day severely. T3 §4.1's 72-hour re-read window and §8 idempotency already absorb this, and the §4.2 high-water-mark design already assumes a fresh read is never final for the current day. Measuring the actual interval is an opportunistic M1 task, not a blocker.
- 2026-07-26 — minor: `aggregateGroupByPeriod` returns `dataOrigins: []` even when a raw read of the same window reports `com.fitbit.FitbitMobile`. Do not use the aggregate result's `dataOrigins` for provenance; use `readRecords` metadata if provenance is ever needed.
- 2026-07-26 — T4: Expo with **Continuous Native Generation** — `android/` is generated by `expo prebuild` and **not committed**. The "managed vs. bare" split is retired terminology in current Expo docs; the live question is only whether to version the native project. Reason: T3 §2's two permission-rationale manifest entries come from `react-native-health-connect`'s config plugin, and a hand-maintained `AndroidManifest.xml` would make T3's own "fails silently" warning permanent. Consequence: New Architecture is mandatory (not disableable from SDK 55 on) and Expo Go is permanently unusable.
- 2026-07-26 — T4: local mirror is **SQLite mirroring tech-01's tables** (`expo-sqlite`, WAL, `synchronous = FULL`), holding the content bundle, player mirror, delta outbox, T3's watermarks and session ledger, and a battle snapshot. `synchronous = FULL` over the usual `NORMAL` specifically because the last transaction is often the delta write that T3 §8.4 orders the watermark advance behind.
- 2026-07-26 — T4: **no server-cache library (TanStack Query et al.).** The API is unreachable by design (T2 §1.2), so there is no cache to invalidate — the mirror is the cache. Zustand holds a hydrated projection only; SQLite is written first, always.
- 2026-07-26 — T4: **level-ups are never optimistically projected**, unlike XP and Leagues (T2 §5). The server owns the curve, and a Reveal Card celebrating a level-up that then retracts is the worst artefact the projection design can produce. Adds a constraint to T2 §5 rather than deviating from it.
- 2026-07-26 — T4: **battle state is snapshotted to SQLite after every resolved turn** and resumed on cold start. Android can kill a backgrounded process with no callback; without this, a phone call mid-boss loses spent items and uses. T5 owns the snapshot's shape.
- 2026-07-26 — T4: asset lookups resolve through a **generated static require map** (`scripts/gen-assets.ts` → `registry.generated.ts`) built from `traverser-data-manifest.md`, with placeholder PNG/OGG files for every key from M0. Metro resolves `require()` at build time, so CLAUDE.md's "manifest key only, no hardcoded filenames" rule is *unimplementable* by any runtime lookup — codegen is the only way to satisfy it. Missing file, orphan file, and wrong filename all fail the build.
- 2026-07-26 — T4: **consequently the two ID families flagged on 2026-07-25 (six `gate_*` keys, twelve concrete `gear_{slot}_{tier}` keys) must be added to the manifest before M0 *builds*, not merely before M0 seeding** — the generator reads the manifest and cannot emit gear requires without the literal keys.
- 2026-07-26 — T4: **GDD 14 §4.1/§4.2's play-once-intro-then-seamless-loop is not satisfiable by `expo-audio` as written** — `loop` loops the whole file and no handoff is gapless on Android by construction. M-phase behaviour: single-file whole-track loop with the intro inside the loop body (intro repeats). Flagged to the audio-production project for a decision with real assets; the fix, if wanted, is contained inside the audio bus. This is the only GDD requirement T4 does not fully meet.
- 2026-07-26 — T4: **uninstall is unrecoverable.** `player_id` (device-minted) and the bearer token both die with the app's storage, so a reinstall is a new guest with no path back to the server-side profile. Falls out of the guest-only trim; recovery arrives with real auth. Named because no prior spec said it out loud.
- 2026-07-26 — **T3 §12 probes 2 (backfill latency) and 9 (long gap) not exercised.** Both are time-elapsed probes whose fallbacks are "widen the constant"; re-reads are idempotent by §8, so the risk of deferring them is low. Worth an opportunistic check during M1 rather than a dedicated spike.
- 2026-07-26 — T5: **the damage formula floors twice, not once.** GDD 2 §7's formula line reads as a single trailing `floor()`, which yields 27 on the super-effective worked example — but GDD 2 §7's own worked example, and fixtures §2, both say **26**, arrived at by flooring the quotient first (`floor(1950/144) = 13`) and then multiplying. Fixtures are the oracle (CLAUDE.md), and the double-floor reading is corroborated by every deterministic value in fixtures §3. Normative pipeline: `base = floor((Power × Atk)/(Def × 8))`, then `max(1, floor(base × surge × type × rend × fortify × weaken × crit × random))`. This is a correction to GDD 2 §7's *notation*, not to its intended behaviour. Note the trap: the two neutral fixture rows pass under either implementation, so only the SE row distinguishes them — pinned by a dedicated test (T5 §11.2).
- 2026-07-26 — T5: **minimum damage is 1.** GDD 2 §7 defines no minimum and fixtures §2 row 4 flags one as owed without fixing a value. GDD 2 §3's "no immunities — every type can always deal some damage" governs; a 0-damage result contradicts it and makes a non-terminating fight reachable (resisted Divine, low Favor, vs. Jörmungandr's Aegis). Clamp applied once, after the final floor.
- 2026-07-26 — T5: **enemies crit too.** GDD 2 §7's "6.25% on every move, no stat or gear interaction" is taken literally. GDD 5–7's fight arcs all model enemy damage without crits, but those are averaged illustrations and none states an exemption. One symmetric code path. The ×4.95 single-hit ceiling this creates is asserted non-lethal against the squishiest enemy in the fixtures (T5 §11.5).
- 2026-07-26 — T5: **Surge and Breach charms persist until a move of their *matching* type resolves.** GDD 4 §3 says an effect "persists until resolved by the qualifying trigger" without defining that trigger for a typed charm. Arming Stormveil and then swinging Iron Advance neither consumes nor boosts. This is the reading that makes charms work as the setup tools GDD 4 §2.3's tactical profile describes.
- 2026-07-26 — T5: **the client rolls loot; the server records it.** `drop_rate`/`enemy_drop_pool` are already client-cached content (T1 §7), and rolling server-side on battle ingest would leave a boss drop nonexistent until the next sync — days, with the API off by design (T2 §1.2) — breaking GDD 13 §6.3's victory-screen Reveal Card. **Requires T2's battle payload to grow a `drops` array**, ingested under the existing `client_battle_id` conflict guard. Flagged to T2; T1's tables and frozen-bonus rule already support it.
- 2026-07-26 — T5: **the tutorial's determinism is a rule flag, not a rigged RNG.** `rules.crit`/`rules.randomFactor = false` skip the draws entirely. A fixed-value RNG would coincidentally give the right answer (0.5 → random 1.0, no crit), which is precisely why it is rejected — fixtures §3 would then pass for a reason unrelated to GDD 10 §6.1. The tutorial test runs on an empty `ScriptedRng` so any draw throws.
- 2026-07-26 — T5: **a battle whose `content_version` or snapshot `schemaVersion` moved underneath it is abandoned and the grant returned unspent**, rather than resumed. Resuming a fight whose move Power changed mid-flight is worse than losing it, and the player keeps the encounter.
- 2026-07-26 — T5: **the eight owed fixtures are delivered as `traverser-test-fixtures.md` §10**, machine-verified by executing the T5 §3.2 pipeline in Node rather than transcribing prose. Covers the double-floor rule (with the four neutral cases that pass under either reading and therefore cannot pin it), the min-damage clamp, crit-on-floored-base, the three ×3.0 ceilings, charm non-consumption, enemy-side crit, turn order, and a full Harpy L15 battle under an explicit 18-value draw sequence (stated as draws, not a PRNG seed, so the oracle is implementation-independent — it also pins T5 §4.1's draw order, since all 18 must be consumed).
- 2026-07-26 — **T2 amended by T5: the battle payload now carries a `drops` array** (new T2 §4.1). Step 2 materialises drops only for the `RETURNING` set, so a replayed battle grants nothing twice; the §6 replay example now asserts 0 duplicate drops; `vigor_after` and `items_consumed` are applied on the same gate. Gear/trinket drops carry their frozen `bonus_primary` and `level_at_drop` — the one place the client supplies a value the server would otherwise derive, which is deliberate because tech-01 §4 freezes the bonus at drop time (re-deriving it server-side would be the bug). Trust boundary stated explicitly in T2 §4.1: a tampered client can mint items and gear and nothing else, since XP/levels/Leagues/gates/grants stay server-derived.
- 2026-07-26 — **GDD 3 and GDD 4's closed "the ×3.0 ceiling never approaches a one-shot" is FALSE for a Favor-specialized build** (fixtures §10.9, verified against real roster stats). Surge+SE at ×3.0, no crit, best SE move for the level: one-shots Satyr at L6 (66 vs 23 Vigor) and L15 (96 vs 45), and Cyclops at L10 (75 vs 60) and L15 (90 vs 82). At the absolute ×4.95 ceiling (×3.0 + crit + roll 1.10) **every enemy in the game one-shots, bosses included** — Cacus L60 takes 188 against 154 Vigor. A *balanced* build matches the GDD's published turn counts closely (Cyclops L15 in 5 SE hits, Cerberus L15 in 6), which is why the original balance modelling missed it: the gap is build specialization, not the formula. **T5 changes nothing** — the formula is locked and implemented as written, and T5 §11.5 asserts a bounded-damage property (`1 ≤ dmg ≤ floor(base × 4.95)`) instead of the survival property, which is not true. Partly self-limiting: a 100%-Favor build sits at Vigor 20 all game. Flagged for a playtest decision; if it needs addressing the levers are Surge's ×1.5 or charm supply, both content-table edits with no engine change.
- 2026-07-26 — minor GDD erratum: **GDD 2 §7's super-effective worked example calls the target "a Sea-aligned enemy (Aegis 18, ×2.0 vs Storm)", but the chart in that same section puts Storm → Sea at 0.5×.** Storm is SE against War and Trickery. The numbers are correct and fixtures §2 is unaffected (it names no types); naming the enemy War- or Trickery-aligned fixes it. One-word documentation fix, no balance implication.
- 2026-07-26 — T5 correction: **gear-granted moves come from the Trinket slot only** (GDD 8 §1), narrowing GDD 3 §4.1's "any Mythic/Divine piece". Weapon/Armor/Accessory are pure stat bonuses at every tier (Weapon→Might, Armor→Resolve, Accessory→Vigor, Trinket→Favor+Aegis split). Affects T5 §6's effect-source column only; the effect model itself is unchanged.
- 2026-07-26 — T6: **the phone reaches the API over Tailscale via `tailscale serve --bg --https=443 http://127.0.0.1:8080`, and Compose binds `127.0.0.1` only.** Confirmed against current Tailscale docs: Personal plan is free (6 users, unlimited user devices, 50 tagged), MagicDNS plus admin-console HTTPS gives a real Let's Encrypt cert for the machine's `*.ts.net` name. Reason for Serve over a raw `100.x` tailnet IP: Android has blocked cleartext HTTP by default since Android 9, so a plain-HTTP endpoint would need a `networkSecurityConfig` exemption — and since `android/` is generated and not committed (T4 §1.1) that exemption would have to become a permanent hand-written config plugin. `--bg` is load-bearing: without it Serve dies with the terminal and needs manual restart after every reboot. Tailscale **Funnel** and router port-forwarding are both rejected as public exposure. Enabling HTTPS publishes machine and tailnet DNS names to a public certificate-transparency ledger — inherent to Let's Encrypt, mitigated by naming the host something uninteresting. No LAN binding, no port-forward, no public DNS.
- 2026-07-26 — T6: **managed cloud Postgres is rejected for the life of the project**, Supabase named as the concrete instance of the pattern. Two independent disqualifications: free-tier inactivity-pause is exactly wrong for a traffic pattern of long silences punctuated by one foreground sync, and free tiers exclude backups, which conflicts with T6 §10 being non-negotiable. Both resolve by paying, violating the $0 constraint. Accepts a worse availability story for a better durability one — losing uptime costs nothing (the client queues, T2 §1.2), losing step history is unrecoverable.
- 2026-07-26 — T6: **Sentry runs on the server too (`Sentry.AspNetCore`), not just the client.** Reason: T4 §8.1 makes the client treat a failing API as success, so a 500 inside `POST /api/v1/sync` is invisible from the phone *by design* — the only other trace is a log line on a PC that has since rebooted. Same free-tier org, two projects, two DSNs. Flagged per CLAUDE.md's cost rule: **the Sentry DSN is the only account/key in the entire stack**; free, no card, self-hostable per GDD 15 §5.2. PII and request-body capture must be explicitly disabled — sync payloads are health data. Analytics trim otherwise unchanged: no tracing, no event pipeline.
- 2026-07-26 — T6: **Postgres backups are `pg_dump -Fc` to local disk plus an already-synced OneDrive/Drive folder, 7 daily / 4 weekly / 12 monthly, mandatory from M1 — before the first real step sync, not after.** Off-machine matters because a dead PC takes every same-machine copy. Trigger is a Task Scheduler task with **both** daily and at-startup triggers and run-if-missed, because a nightly cron on a machine that is asleep at 3am is not a schedule (T6 §1.1). Restore drill (`pg_restore` into a scratch DB, spot-check, `dropdb`) runs at M1 and M5 — an untested backup is not a backup. Backup set also includes `.env` (holds the Postgres password) and the release keystore.
- 2026-07-26 — T6: **a stable out-of-tree release keystore is mandatory, not good practice — and this is a consequence of T4 §1.1 that T4 does not state.** `expo prebuild --clean` regenerates the *debug* keystore, so anything signed with it has a silently-changing signing identity; Android refuses to update-install across a key change, the only way forward is uninstall, and per T4 §6.5 uninstall destroys `player_id` and the bearer token unrecoverably. Therefore: one `keytool` keystore generated at M0, stored outside `android/` (which prebuild deletes), passwords from the environment, signing config injected by a local Expo config plugin, and the keystore itself in the backup set. Release artefact is an **APK**, not an AAB (AAB cannot be sideloaded).
- 2026-07-26 — T6: **backing up Postgres alone is not restorable-to** — `player_id` and the token live only in app storage (T4 §6.5), so a restored database has no client able to claim it. Opened as T6 §13.1 rather than decided silently. **Recommendation: a manual `player_id`/token export-and-restore path at M1**, alongside the backup job — a screen, a file, and a branch in registration. The recovery-code alternative arrives with real auth (T2 §8) and should not be built before it. T4 §15 deferring "account recovery after uninstall" was right as a *feature* decision but leaves T6's backup deliverable incomplete.
- 2026-07-26 — T6: **Docker Desktop on the WSL2 backend hosts Compose; two services only (`api`, `db`), no reverse proxy, no pgAdmin, no self-hosted Sentry.** Docker-in-WSL2-directly was rejected because WSL2's separate network namespace complicates tailnet reachability of published ports, whereas Docker Desktop publishes to Windows localhost, which is exactly what the Windows `tailscale serve` client proxies. `restart: unless-stopped` rather than `always`, so the stack returns on boot but respects a deliberate `docker compose stop`.
- 2026-07-26 — T6: **.NET 10 container images are Ubuntu 24.04 Noble and Debian images are not shipped for .NET 10 at all.** `mcr.microsoft.com/dotnet/{sdk,aspnet}:10.0` resolve to Noble; `-bookworm-slim` does not exist and a copied tutorial Dockerfile using it will fail to pull. Documented .NET 10 breaking change, recorded because it will look like a typo.
- 2026-07-26 — T6: **migrations and content seeding are explicit commands, never application startup work**, and the API asserts its expected migration set on boot and refuses to serve if it is wrong. Reason: this stack stops on a desktop's power button, so "began migrating, machine slept" is reachable, and a half-applied migration on the only copy of the fitness history is worse than a refusal to start. **T5 §12's content-bundle validation pass lands in the seed step** (T6 §5.4) — every enemy has ≥1 move, all `enemy_move.weight` strictly positive, drop-pool FKs resolve, every content key exists in the manifest, all 60 `xp_curve` rows present, `grants_move_id` set only on Trinket gear. This is the server-side twin of T4 §9.2's build-time asset check.
- 2026-07-26 — T6: **`EXPO_PUBLIC_API_BASE_URL` is build-time config sourced from `app.config.ts`, and the tailnet HTTPS URL is used for dev builds as well as release.** It cannot live in `gradle.properties`/`AndroidManifest.xml` — prebuild deletes them (T4 §1.1). Using the LAN address in dev is rejected as a default because it reintroduces the cleartext problem and means the shipped configuration is never the one exercised; kept as a documented fallback for isolating whether a failure is Tailscale's or the API's. Adds one row to T4 §3.2's rebuild-boundary list: changing the API host is a rebuild, not a reload — which is also why moving hosts (T6 §11) requires a resigned APK.
- 2026-07-26 — Phase 1 close-out: **the manifest gained the two ID families flagged 2026-07-25** — six `gate_*` keys (new §Zone Gates section, thresholds per fixtures §8) and the twelve concrete `gear_{slot}_{tier}` keys replacing the pattern row. Also expanded the `stg_reveal_*` wildcard to `stg_reveal_1/2/3`, which were already canonical in GDD 14 §3.5 (manifest rule 4: GDD wins) — T4 §9.2's codegen cannot expand a wildcard any more than it can a pattern. M0's build and seed blockers are cleared.
- 2026-07-26 — Phase 1 close-out: **T3 amended in place with the spike corrections** (§3 reads-throw-not-empty, per-process `initialize`, StepsCadence exact-match; §4 pagination, per-minute HR records, UTC-instant filter strings, probes 4/5 confirmations; §12 status note), following the T2-amended-by-T5 precedent so no spec page is left stating something the spike disproved. This log remains the discovery record; the spec is now self-consistent with it.
- 2026-07-26 — Phase 1 close-out: **T3 §5.3's session-close boundary tightened** — a gap of *exactly* 10 sub-Tier-1 minutes does **not** close a session; the 11th consecutive minute does. GDD 11 §8.1's "more than 10" always said so; T3's "after 10 consecutive" phrasing left the boundary ambiguous. Pinned by fixtures §11.3's boundary row.
- 2026-07-26 — Phase 1 close-out: **the eight fixtures T3 §10 owed are delivered as `traverser-test-fixtures.md` §11**, machine-verified by executing the T3 §5/§8 algorithms in Node (same method as T5's §10). Age 55 pins the `ceil` thresholds; §11.6 pins the client-uncapped/server-capped Tier 3 split with both failure modes quantified; §11.7 pins the never-lower-the-mark rule; §11.8 pins earlier-ID-wins session merging. Phase 1 now ends with zero owed artifacts.
- 2026-07-31 — M0/T6: **the API's connection string when run from the host (`dotnet run`, `dotnet ef`) comes from .NET user-secrets**, not from `infra/.env` or a committed `appsettings` file. T6 §4.1 covers the Compose case only (`ConnectionStrings__Traverser` in the environment), and T6 §5.1's "run `dotnet ef database update` from the host" needs a home for that credential which is neither the repo nor a file the app never reads. `appsettings.Development.json` keeps the non-secret half (host, database, username) so the shape stays discoverable, with an empty password the secret overrides. `.env` is unchanged and still configures Postgres itself plus the API container — the two are not interchangeable, and `.env` stays in the §10.5 backup set. Consequence: a fresh clone needs one `dotnet user-secrets set` before `dotnet ef` works; M0's README owes that line. `.env.example` was also blanked to match T6 §2's "every key present, every value blank" — its stale filled-in values had already caused one wrong-database run.
- 2026-07-31 — M0/T1: the EF model **adds the single-column foreign-key indexes EF Core creates by convention** (`ix_enemy_type_id`, `ix_player_item_item_def_id`, …), so the generated schema carries more indexes than tech-01 §3/§4 lists. Kept rather than removing `ForeignKeyIndexConvention`: they cost nothing at single-player write volume, they help the `on delete cascade` paths, and disabling a global EF convention is a bigger surprise for future tables than the extra indexes are. Every index tech-01 *does* name is present and explicit.
- 2026-07-31 — M0/T1: `EFCore.NamingConventions` **does not break a word at a digit boundary** — `Tier1Minutes` maps to `tier1minutes`, not tech-01's `tier1_minutes`. `activity_day` and `hr_session` therefore carry explicit `HasColumnName` calls for the six tier-minute columns. Any future column with a digit in its name needs the same treatment.
- 2026-08-01 — M0/T1 seed: **gear stat bonuses require banker's rounding (`MidpointRounding.ToEven`), and this is load-bearing rather than incidental.** Fixtures §5 needs Mortal at L10 = 1 (`round(0.5)` → 0) and Divine at L42 = 14 (`round(10.5)` → 10); rounding halves away from zero yields 2 and 15, so 4 of the 53 reference values break. .NET's `Math.Round` default is already correct — **JavaScript's `Math.round` is not**, and the client computes gear bonuses at drop time. This is the same client/server rounding hazard tech-01 §5 called out for `xp_curve`, which it solved by seeding the curve; there is no equivalent table to seed here, so T4/T5 must use an explicit round-half-to-even helper and never `Math.round`. Verified: one rounding rule (ToEven) satisfies fixtures §4 and §5 together — for the XP curve the two modes agree at all 59 levels, so nothing is traded away by standardising on it.
- 2026-08-01 — M0/T1 seed: **`zone.is_released` drops tech-01 §3's `default true`.** EF Core's `HasData` omits a column whose seeded value equals the CLR default, *regardless of `HasSentinel`*, so `egypt_tbd`'s `false` was left out of the generated `InsertData` and the store default wrote `true` — the Map's locked terminus (GDD 9 §3.1) shipped unlocked, and every model-level test still passed because the model was right and only the emitted SQL was wrong. `zone` is seeded and read-only, so the seed is its only writer and a store-side default has no runtime role; the CLR initializer still defaults new rows to true. Guarded generally by a test asserting no seeded value conflicts with a store default on its column — the rule being: a seeded column may carry a store default only if every seeded row agrees with it.
- 2026-08-01 — M0/T1 seed: **three values the GDD does not specify, chosen and flagged.** (1) `zone.egypt_tbd.display_name` = "The Road Ahead" — the manifest registers the key but no display name, and GDD 9 §6 leaves the zone name TBD; this is a placeholder for a locked node the player can see, and wants a real decision before any UI ships. (2) The Waystone Wisp's Might and Aegis = 10 (Level 1 baseline) — fixtures §3 and GDD 10 §6.2 pin only Vigor 15 / Resolve 8 / Favor 12 / Stride 6; both unspecified stats are unreachable in the scripted tutorial (the Wisp's only move is Divine so its Might never applies; the player's only action is Physical so the Wisp's Aegis never applies), and 10 rather than 0 keeps them out of a divisor. (3) `emove_chilling_gust.ai_weight` = 100 — the manifest says "scripted"; it is the enemy's only move, so 100 is both literally true and what keeps the sum-to-100 seed test uniform across all 13 enemies.
- 2026-08-01 — M0/T1 seed: **`drop_rate` seeds 13 rows and `enemy_drop_pool` 47, against tech-01 §5's "~10" and "~40" estimates.** Both are the enumerated actuals, not additions: `drop_rate` is every (encounter_kind, reward_kind) pair that exists, and a *missing* row is meaningful — wild encounters and the daily goal have no `trinket` row because Trinkets never drop from either (GDD 8 §5.2). Mini-boss pools carry one `mini_boss` row set covering both first and repeat kills, since GDD 5 §3.3 gives them the same pool. All `enemy_drop_pool.weight` values are 1: the GDD describes these pools as evenly weighted, and the two "always included" first-kill Rares (Fleet Omen, Ambrosia Shard) are a battle-engine guarantee for T5, not a weighting.
- 2026-08-01 — **T1 amended in place with four schema additions later specs require and it does not provide**, following the T2-amended-by-T5 and T3-amended-post-spike precedent. Found while building the M0 EF model: T1 was written first, so where T2/T3 resolved something by inventing storage, T1 has no column for it. (1) `player_settings.birth_year` — GDD 1 §2.2 needs `HRmax = 220 − age`, GDD 10 never collects it, T3 §1.4 closes the gap at Screen 3 and T4 §6.4 puts it here; nullable because registration precedes Screen 3, and null means *thresholds not derivable*, not age 0. (2) `encounter_grant` + `battle.grant_id` — T2 §1.3's whole offline-battle seam, plus the `grant_id` in its battle payload and the `grant_already_spent` error, had no table. (3) `auth_token` — T2 §1.4's bearer token had nowhere to live. (4) `client_operation` — T2 §2 requires every write to carry a retry-safe client ID; `sync_delta` covered sync and `client_battle_id` covered battles, but T2 §3's progression writes had no ledger. Nothing in §3 (content) or §5 (seed plan) changed.
- 2026-08-01 — T1 amendment, **grant redemption is derived, not stored**: `encounter_grant` has no `spent_at`. A grant is spent iff a `battle` row references it, enforced by `create unique index on battle (grant_id) where grant_id is not null`. Reason: T1 §2's "derived values are not stored", and a `spent_at` next to a `battle.grant_id` is two places holding one fact that can disagree. Double-spend becomes structurally impossible rather than a check to remember — same move as `player_gear`'s one-per-slot partial index. T2 §6.2's `grant_already_spent` is then the unique violation, and T5 §8.1's abandoned-battle grant return needs nothing at all (no battle row is written, so the grant stays unspent by derivation).
- 2026-08-01 — T1 amendment, **`auth_token` stores a SHA-256 hash, never the token**. Reason: it is a no-expiry credential and T6 §10.5 copies `pg_dump` output into an already-synced cloud folder nightly, so plaintext would replicate the credential off-machine on a schedule. Unsalted is correct *because* the token is server-minted high-entropy random rather than user-chosen — there is no dictionary to attack, and per-row salt would break the O(1) lookup the auth path needs. `revoked_at` rather than a delete keeps "de-authorised" distinguishable from "never existed" when something 401s. `last_used_at` is diagnostics only and must not become session expiry without a spec change.
- 2026-08-01 — T1 amendment, **`client_operation` covers only the additive/effectful progression writes**, not all of T2 §3. `PUT /skills` and the equip toggle are last-write-wins (T2 §6.3) and `pending-rewards/{id}` is idempotent on its own `resolved_at`, so a ledger row for them would be ceremony. The ones that need it: `POST /allocations` (a replay would silently re-add stat points), item discard, rest-day tagging, Explore requests. **Zero rows from `ON CONFLICT DO NOTHING` is success, not an error** — T2 §3's "rejected" for allocations means the points aren't added twice, not that the client sees a failure; the response is current state, which T2 §7 already guarantees is repairable from `GET /players/me` in one shot. No response body is cached.
- 2026-08-01 — M0/T6 §3, **correction to the spec's Compose table: the `db` volume mounts at `/var/lib/postgresql`, not `/var/lib/postgresql/data`.** The 18+ official images moved `PGDATA` into a major-scoped subdirectory (`/var/lib/postgresql/18/docker`) so `pg_upgrade --link` can see two majors inside one mount point; mounting the pre-18 path makes the container refuse to start with "there appears to be PostgreSQL data in /var/lib/postgresql/data (unused mount/volume)". T6 §3's table was written against the pre-18 convention that every tutorial still shows. Found by hitting it. The payoff is real rather than cosmetic: the next major upgrade is a `pg_upgrade`, not a dump/restore, and by M1 the volume holds fitness history.
- 2026-08-01 — M0/T6 §3.2 **deviation: `db` publishes `127.0.0.1:${POSTGRES_PORT}:5432`** where the spec says it publishes no port at all. T6 §5.1 chose `dotnet ef database update` *from the host* as the migration path, which needs a reachable Postgres, and §6.3's "psql inside the container" answer covers neither EF tooling nor a GUI client. Loopback-bound, so §1.2's "nothing on the LAN reaches it" still holds — verified: the API answers on 127.0.0.1:8080 and the host's LAN address refuses. The spec's reasoning (don't publish out of habit) is respected; its conclusion is not reachable given §5.1.
- 2026-08-01 — M0/T6: **Postgres 16 → 18 done now, at the last moment it is free.** The scaffold pinned 16; T6 §3 specifies 18 (confirmed current major 2026-08-01). A major bump is a data-directory incompatibility, so it costs a reseed today and a `pg_upgrade` from M1 on, when §10.7 says the data starts mattering. The old `infra_postgres_data` volume was **orphaned, not deleted** — the volume rename to `traverser_pgdata` means the PG16 data is still on disk if anything needs checking; `docker volume rm infra_postgres_data` when it doesn't.
- 2026-08-01 — M0/T6 §5.1 **decided: migrations run from the host** (`dotnet ef database update --project api/Traverser.Api`), not via a one-shot `migrate` Compose profile. §5.1 offered both and required a pick. The host path is already the established one (user-secrets holds the host connection string, DECISIONS 2026-07-31), it keeps the EF Core Design tooling out of the runtime image, and it is what the inner loop already does. Consequence: this is the second thing depending on the published `db` port above.
- 2026-08-01 — M0/T6 §3.5: the runtime stage is **`aspnet:10.0-noble-chiseled-extra`, not plain `-noble-chiseled`.** Plain chiseled omits ICU *and* tzdata, and Npgsql resolves time zones through `TimeZoneInfo` when reading a `timestamptz` — of which tech-01's schema is full. Saving ~15 MB would buy a `TimeZoneNotFoundException` thrown from inside a query rather than at startup. Confirmed non-root (UID 1654) and genuinely distroless (`exec id` fails: no shell, no coreutils). One cosmetic consequence: Npgsql logs `Cannot load library libgssapi_krb5.so.2` once per connection attempt because chiseled has no Kerberos libraries; the connection succeeds over SCRAM regardless and there is nothing to fix — do not chase it.
- 2026-08-01 — M0/T6 §4.1: **`ConnectionStrings__Traverser` is assembled in `docker-compose.yml`, not stored as a key in `.env`.** §4.1's table lists it as a `.env` key whose POSTGRES_* duplication "Compose interpolation keeps from drifting"; building it in the `environment:` block *is* that interpolation, and it removes the possibility of a hand-edited `.env` disagreeing with the values right above it. `.env.example` therefore no longer carries the key, and says why — a blank key nothing reads is worse than no key.
- 2026-08-01 — M0/T2 §3: **`GET /api/v1/content/version` returns `{"content_version": 1}`, an object, where the spec says "a single integer".** Everything else on tech-02's wire is a snake_case JSON body, and a bare scalar response has nowhere to put a second field if the bundle poll ever needs one. Also **unauthenticated for now**: tech-02 §1.4's bearer token has no storage until the amended `auth_token` table is built, and this returns one integer about seeded content rather than player data. Revisit when registration lands — it is the only endpoint on the surface where staying unauthenticated would be defensible, and that should be a decision rather than an oversight.
- 2026-08-01 — M0: **`Microsoft.OpenApi` pinned to 2.11.0**, same transitive-pin pattern as `Microsoft.EntityFrameworkCore.Relational`. `Microsoft.AspNetCore.OpenApi` 10.0.10 still resolves 2.0.0, which carries GHSA-v5pm-xwqc-g5wc (high, patched 2.7.5). The advisory is a parser denial-of-service and this app only *generates* OpenAPI documents, so real exposure is nil — pinned anyway, because a permanent NU1903 on every build is exactly how a genuine advisory later gets scrolled past. Build is now warning-clean.
- 2026-08-01 — M0/T4: **the Android package name is `com.oldroads.traverser`**, set before the first `expo run:android` because it cannot be set after one. The package is the app's identity to Android; changing it post-install makes the new build a different app, and T4 §6.5 makes uninstall total (SQLite mirror, T3 watermarks, Health Connect grants). Reverse-DNS is convention only — Play verifies uniqueness, never domain ownership — so the choice was between a personal name and a project one, and the project one wins on the merits: the package is permanently visible (Play URL, `pm list packages`, every Sentry event) and there is no reason to make a surname unchangeably public for a $0 personal app. `com.anonymous.*` (the Expo template default) and `com.example.*` are both rejected by Play, which is why the template ships no package at all. App name `Traverser`, slug and scheme `traverser`, replacing the template's `app`/`app`/`app`.
- 2026-08-01 — M0/T6 §4.2: **`app.json` is deleted and replaced by `app.config.ts` outright, not spread into it.** The documented Expo pattern layers a dynamic `app.config.ts` over a static `app.json` via `ConfigContext`, but when both exist the dynamic file wins and the static one contributes *only what is explicitly spread* — so a stale unspread `app.json` sitting in the repo is a silent-drift trap for exactly the values (package, scheme) that must never drift. One file, no precedence question. `EXPO_PUBLIC_API_BASE_URL` is **required**: an unset value throws during config resolution rather than defaulting, because a default produces an app that builds and installs cleanly and then fails every sync on-device, which is the most expensive place to find a missing variable. Trailing slash is normalised off in the config so callers can join leading-slash paths unconditionally; `app/src/env.ts` is the only reader of `Constants.expoConfig.extra`.
- 2026-08-01 — M0/T6 §8.2, **the tailnet host is renamed `workshop` (was `mnjd-desktop`), done before enabling HTTPS rather than after.** §8.2 already said the mitigation for the certificate-transparency disclosure is naming the host something uninteresting; it did not say the mitigation has an expiry. A CT-log entry cannot be withdrawn, so the rename is only free while no certificate has ever been issued. Second reason for the ordering: the MagicDNS name *is* the client's base URL and §4.2 makes that build-time, so a rename after the first build is a rebuild and a reinstall. `tailscale set --hostname workshop` does it from the CLI, no admin console. Client base URL is therefore `https://workshop.tail465912.ts.net/api/v1`. The tailnet half (`tail465912`) is Tailscale-generated and already uninteresting. Note this is per-machine: the planned home-lab box gets its own name and its own §11 migration.
- 2026-08-01 — M0/T6 §8.2, **correction: there are three admin-console prerequisites, not two.** Beyond MagicDNS and HTTPS certificates, **Serve itself must be enabled for the tailnet** via a node-scoped consent link. The failure mode is why this is worth recording: `tailscale serve --bg --https=443 http://127.0.0.1:8080` does not exit with an error when Serve is disabled — it prints `Serve is not enabled on your tailnet` plus a `login.tailscale.com/f/serve?node=…` URL and then **blocks indefinitely**, waiting for the toggle. A hanging command reads as a network or firewall problem, which is the wrong place to look. Found by running it.
- 2026-08-01 — M0/T6 §9: **Sentry wired on both tiers, errors only, with every privacy option set explicitly rather than left at its default.** §9.3's ⟨verify⟩ resolved — server: `SendDefaultPii = false`, `MaxRequestBodySize = RequestSize.None` (`Sentry.Extensibility`), `TracesSampleRate = 0.0`; client (`@sentry/react-native` 7.11.0, names checked against the installed typings): `sendDefaultPii`, `attachScreenshot`, `attachViewHierarchy` all false, `tracesSampleRate`/`profilesSampleRate` 0. Blank DSN disables capture on both tiers, so the dev loop still runs with no Sentry account — verified: the API boots and serves with `Sentry__Dsn` empty. Client config reaches `src/sentry.ts` through `app.config.ts` → `extra` → `src/env.ts`, the same single-read-point path as the API base URL, adding `EXPO_PUBLIC_SENTRY_DSN` and `EXPO_PUBLIC_SENTRY_ENVIRONMENT` to what §4.2 called a one-key surface.
- 2026-08-01 — M0/T6 §9.2, **the migration-assertion failure needs an explicit `SentrySdk.CaptureException` + `FlushAsync`, not the ASP.NET Core integration.** §9.2 names it as one of three things the server must capture, but Sentry's integration reports unhandled exceptions from the *request pipeline* and the §5.2 assertion throws at startup, before a pipeline exists — so the one failure the spec asks for by name would have been the only one never reported, and the process would exit before the background sender flushed anyway. Caught while implementing, not by a test; there is no test for it, because asserting "an event reached Sentry" needs either a network round-trip or a transport fake, and neither is worth it at this scale.
- 2026-08-01 — M0/T6 §9.3, **`enableCaptureFailedRequests: false` on the client, for an architectural reason rather than a privacy one.** T2 §1.2 makes an unreachable API the normal case and T4 §8.1 makes the client treat it as success — a failed request is the offline design working correctly. Left at the SDK default it would spend the free-tier quota on events describing correct behaviour and bury real crashes. Same principle as §9.4's "the API being down is not an incident", applied to the tier §9.4 does not cover.
- 2026-08-01 — M0/T6 §9, **source-map upload is deliberately off** (`disableAutoUpload: true` on the `@sentry/react-native` Expo plugin). Enabling it needs a `SENTRY_AUTH_TOKEN` in the build environment *plus* the `@sentry/cli` binary whose postinstall npm blocks by policy — a second credential and a second install step, for a benefit that only exists in a release build since dev bundles are not minified. Consequence to accept knowingly: **JS stack traces from a release APK will be minified until this is turned on**, so it is an M5 item, not a never. The plugin logs `Missing config for organization, project` on every build as a result; that warning is this decision, not a misconfiguration. `metro.config.js` (`getSentryExpoConfig`) is the other half and was likewise not added — the project has no `metro.config.js` at all, and introducing one that could perturb bundling buys nothing while upload is off.
- 2026-08-01 — M0/T6 §9, **`Sentry.AspNetCore` distinguishes an absent DSN from an empty one, and only the empty one means "disabled".** A null DSN throws `You must supply a DSN to use Sentry` at host build, so wiring `UseSentry` broke `dotnet ef` and `dotnet run` on the host while the container stayed healthy — Compose always passes the key via `${Sentry__Dsn:-}`, so in Docker the value is present-and-empty, but on the host it is simply not configured. Fixed by normalising in code: `options.Dsn = builder.Configuration["Sentry:Dsn"] ?? string.Empty`. Worth recording because §4.1's "blank is valid and disables capture" reads as true by default and is not — it is true only where something guarantees the key exists, and the host is not such a place. Found by `dotnet ef migrations list` failing, not by a test.
- 2026-08-01 — M0/T6 §4.1, **rotating `POSTGRES_PASSWORD` in `.env` does not change the database password**, and this is a trap with a delay on it. The official image applies `POSTGRES_PASSWORD` only when it *initialises* a fresh data directory; against an existing volume the role keeps its old password, so editing `.env` silently desynchronises three things — the running container's env, the role in the database, and the host's user-secrets copy. The symptom is deferred: the running API keeps working on its established connection pool and only fails on the next container recreate. Procedure recorded in the README: recreate `db` so its env updates, `ALTER ROLE CURRENT_USER WITH PASSWORD` through the container's unix socket (which is `trust`, so neither password is needed), recreate `api`, then re-set user-secrets. Matters more from M1 on, when the volume holds fitness history and a reseed stops being a free way out.
- 2026-08-01 — M0/T6 §5.4, **the validation pass splits between CHECK constraints and tests, because "it lands in the seed step" has no target.** The seed is EF `HasData` inside a migration, so there is no seed step to hook. Per-row facts became constraints in migration `20260801140924_ContentValidationConstraints` (`ck_enemy_move_ai_weight` tightened, `ck_drop_rate_chance`, `ck_enemy_drop_pool_weight`, `ck_gear_def_grants_move_trinket_only`), failing when the migration is applied; cross-row and cross-file facts became `ContentValidationTests`, failing at `dotnet test`. All four constraints applied against the live seeded database with zero violations, so they guard future edits rather than fixing present data. **`ck_enemy_move_ai_weight` had permitted exactly the value §5.4 forbids** — it read `between 0 and 100` and now reads `between 1 and 100`; a zero-weight move is not rare, it is unselectable by T5 §5's weighted pick, so the enemy would quietly play a smaller movepool than its data claims.
- 2026-08-01 — M0/T6 §5.4, **two checks the section's list left implicit.** (1) `drop_rate.chance` excludes **0** as well as values above 1: "never drops" is expressed by *omitting the row* — GDD 8 §5.2 gives wild encounters and the daily goal no `trinket` row at all, and DECISIONS 2026-08-01 already records that a missing row is meaningful there — so a `0.0` row would be a second spelling of an absence that already carries meaning, and two spellings of one fact is how they come to disagree. (2) `enemy_drop_pool.weight` needs the same strict positivity as `enemy_move.ai_weight`, for the same weighted-draw reason; §5.4 named the move case only. An enemy with *no* pool rows stays legal and meaningful (`enemy_waystone_wisp` falls back to the generic pool, which is how fixtures §3's "Drops: None" needs no special case) — a row that exists but can never be drawn does not.
- 2026-08-01 — M0/T6 §5.4, **the manifest cross-check parses `traverser-data-manifest.md` rather than transcribing it, the opposite of the fixtures' approach, deliberately.** `Fixtures.cs` transcribes ~50 numbers *because* an independent second copy is the point — it is the oracle. The manifest is a registry of ~200 identifiers where the goal is to be the *same* copy, so a transcribed list would drift and begin reporting its own transcription errors as seed errors. Entity selection is by shape (any seeded table with a single string primary key) rather than an explicit list, so it fails closed: a content table added later is covered without anyone remembering to register it, which is the exact failure mode the check exists to prevent. Guarded by a floor assertion on the number of IDs examined, so a metadata-API change that made the filter match nothing fails loudly instead of passing vacuously. Verified by mutation: renaming one seeded ID fails the test naming that ID. Direction is one-way by design — the manifest also registers audio keys and reserved analytics names that correctly have no database row.
- 2026-08-01 — M0/T6 §7.3, **release keystore generated: `traverser-release.keystore` at the repo root, 4096-bit RSA, alias `traverser`, `CN=Traverser, O=Old Roads`, valid to 2053, PKCS12.** Gitignored via a new `*.keystore`/`*.jks` rule added to the root `.gitignore` *before* the file was created, so it was never briefly visible to git — the root ignore file had no keystore rule at all (only `app/.gitignore` did, and it does not cover the repo root). Passwords are machine-generated rather than human-chosen, since this is a ~27-year credential. ↯ **PKCS12 does not support a separate key password** — keytool warns and silently reuses the store password, so `TRAVERSER_KEY_PASSWORD` deliberately equals `TRAVERSER_KEYSTORE_PASSWORD`; setting them differently produces a keystore that Gradle cannot open with the values you think you configured. §10.5's backup set now has a third member alongside `.env`: the keystore *and* `~/.gradle/gradle.properties`, since either alone is useless.
- 2026-08-01 — M0/T6 §7.3 **deviation: signing credentials live in `~/.gradle/gradle.properties`, not "the environment" literally.** The spec said passwords come from the environment; Gradle reads project properties from that file *and* from `ORG_GRADLE_PROJECT_*` environment variables, so the plugin's `project.findProperty` supports both and the file is just the persistent default. Chosen because a shell-export approach silently reverts to the debug key the moment someone builds from a terminal that lacks the exports — the failure is a wrongly-signed APK, not an error. The home directory also survives `prebuild --clean`, which is the whole point of keeping this out of `android/`.
- 2026-08-01 — M0/T6 §7.3, **the ⟨verify the plugin API surface⟩ marker resolved: `withAppBuildGradle` from `expo/config-plugins`, appending a marker-guarded Groovy block.** Rejected regex-editing the template's existing `signingConfigs {}` block as too fragile across SDK upgrades; appending a block that calls `android.signingConfigs.create(...)` and reassigns `android.buildTypes.release.signingConfig` achieves the same thing without depending on the template's formatting. The marker comment makes re-running prebuild without `--clean` idempotent. **The default this replaces is the hazard itself, now confirmed verbatim in the generated file: `release { signingConfig signingConfigs.debug }`, carrying the template's own "Caution!" comment** — and since prebuild regenerates `debug.keystore`, every clean rebuild would otherwise ship a release APK with a different signing identity. The injected block **throws** when a release task is requested and the keystore properties are absent, rather than falling back: a silent fallback produces an APK that installs fine today and cannot be updated tomorrow.
- 2026-08-01 — M0/T6 §7.2, **⟨verify the exact flag spelling on SDK 57⟩ resolved: `npx expo run:android --variant release` is correct** (`--variant <name>`, default `debug`, per the SDK 57 CLI's own help). No change to the spec's wording.
- 2026-08-01 — M0 close-out review: **the four T1 amendments of 2026-08-01 (`auth_token`, `encounter_grant` + `battle.grant_id`, `client_operation`, `player_settings.birth_year`) had been amended into the spec but never built into the EF model** — the close-out's "EF schema delivered" claim was true only of the pre-amendment schema. Built as migration `20260802002241_T1AmendmentTables`, matching the amended spec exactly: hashed-token PK, composite FK to `activity_day`, the `battle (grant_id)` partial unique index that derives "spent", and the `ck_player_settings_birth_year` sanity CHECK. No new decisions — transcription of what the spec already said.
- 2026-08-01 — M0 close-out/T4 §9.2–9.3: **asset pipeline built; audio placeholders are silent `.wav`, not §9.3's `.ogg`, and Metro cannot bundle `.ogg` at all without config.** No OGG encoder exists in the $0 toolchain, and Metro's default `assetExts` includes `wav` but not `ogg` — so the spec's `require('….ogg')` was unbundleable as written regardless of placeholders. The generated registry prefers `{key}.ogg` over `{key}.wav` per key (both at once is an error), making real audio deliveries drop-in; when the first real OGG lands, `metro.config.js` must add `ogg` to `assetExts` — deliberately deferred to M5, which introduces that file anyway for `getSentryExpoConfig`. T4 §9.3 amended in place. Placeholder PNGs are flat-colour with the key rendered in a bitmap font, per spec; 52 sprites + 63 audio files, all 115 generated.
- 2026-08-01 — M0 close-out/T4 §2: **`noUncheckedIndexedAccess: true` added to `app/tsconfig.json`** — §2 required it alongside `strict` and the M0 scaffold had only `strict`. Template code that would have violated it was deleted in the same change (the app is now the minimal boot shell: root layout with the Sentry wrap, one placeholder screen, `env.ts`/`sentry.ts`). The template's iOS/web config blocks and assets were removed with it (Android-only, CLAUDE.md); the template's web-facing npm dependencies (`react-native-web`, `react-dom`, `expo-web-browser`, `@expo/ui`, `expo-glass-effect`, `expo-symbols`) are still installed and left for a deliberate prune at the next device rebuild, not removed blind in a cleanup pass.
- 2026-07-26 — **T6 §13.1 decided: candidate 1.** A manual `player_id`/bearer-token export (Settings screen → file in the §10.5 backup set) plus a restore branch in first-launch registration, built at **M1 alongside the backup job**. Candidate 2 (recovery code) waits for real auth; candidate 3 (accept the loss) rejected as incompatible with §10's non-negotiable framing. This closes T6's only open item; M1 planning must include it.
- 2026-08-01 — M0/T6 §7.1, **environment fix, not a spec change: the Android SDK's bundled ninja 1.10.2 is replaced by 1.12.1 at `Sdk/cmake/3.22.1/bin/ninja.exe`** (original retained beside it as `ninja-1.10.2-original.exe`). The first native build cannot succeed without this. 1.10.2 on Windows intermittently fails to `stat` an existing file — here Reanimated's `react-native-workletsConfigVersion.cmake` prefab dependency — so ninja treats `build.ninja` as permanently stale, re-runs CMake, receives an identical manifest, and loops to `ninja: error: manifest 'build.ninja' still dirty after 100 tries`. Proven by elimination rather than guesswork: `cmd` stats the file, a standalone one-off ninja manifest stats the *same* 264-character relative path without complaint, and 1.12.1 reports nothing missing where 1.10.2 reports exactly one file. **Path length is not the trigger** despite the failing path being the longest in the dependency set, and `LongPathsEnabled=1` (now set, and wanted by Reanimated's Windows guide regardless) changes nothing for ninja, whose manifest does not opt into long paths. Also ruled out: clearing every `.cxx` cache, `subst` to a short drive (Gradle canonicalises back), `CMAKE_VERSION=3.31.1` (Reanimated 4.5.1 does not read it), and deleting `CONFIGURE_DEPENDS` from Reanimated's `CMakeLists.txt`. ↯ An SDK Manager update to the CMake package silently restores 1.10.2 and the loop returns; documented in the README with the restore path.
- 2026-08-02 — M1/P1/T3 §2, **correction: `react-native-health-connect`'s own config plugin delivers one of §2's two rationale entries, not both.** `m1-plan` §P1 says registering the library's plugin "adds two manifest entries"; its `app.plugin.js` pushes only the `androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE` intent-filter onto MainActivity — the Android 13-and-below path. The `<activity-alias android:name="ViewPermissionUsageActivity">` that Android 14+ uses is absent, and the Pixel is Android 15, so the entry the library supplies is precisely the one that never fires on the target device. Added as `app/plugins/withHealthConnectRationale.ts`, second local plugin after `withReleaseSigning` and for the same reason (T4 §1.1 — prebuild owns `android/`, so a hand-edit is a write to a temp directory). ↯ The failure mode is why this is worth a plugin rather than a note: a missing rationale target does not throw and does not fail the build, it makes the privacy-policy link inside Health Connect's own permission dialog dead. Nothing in the app can observe it. Sideloading makes that a broken link today and a Play-review rejection the day distribution changes. The alias targets MainActivity rather than a dedicated Kotlin `PermissionsRationaleActivity`, which is the same trade the library makes for its own filter and keeps the native project fully generated. The library is pinned to an exact `3.5.3` rather than `^3.5.3`: T3's four spike-derived ↯ behaviours (permissions authority, throwing reads, pagination, `initialize` every pass) are observations about a version, and a minor bump arriving through `npm install` would revalidate none of them.
- 2026-08-02 — M1/P1/T4 §3.1: **`minSdkVersion` is raised 24 → 26 via `expo-build-properties`.** Not a preference — `androidx.health.connect:connect-client` declares 26 and Expo SDK 57 defaults to 24, so without it the manifest merger fails the build outright. Nothing is given up: Health Connect needs Android 8.0+ even on the installable-APK path and is only part of the OS from 14, so every device that could run the health integration already clears 26. This is the first entry in a `plugins` array that now carries a build-properties block, and it is a rebuild boundary in the T4 §3.2 sense.
- 2026-08-02 — M1/P1/T4 §6.4: **`expo-secure-store`'s config plugin is registered rather than left off.** It sets `android:fullBackupContent` and `android:dataExtractionRules` to exclude the store from Android Auto Backup. ↯ Not cosmetic and not a web-side concern: SecureStore values are encrypted with a device-bound Keystore key, so a backed-up blob restored onto another device is undecryptable — the app would hold a bearer token it can never read, which is worse than holding none, since "no token" is a state registration recovers from. T6 §13.1's identity export is the supported recovery path and Auto Backup must not look like a second one. By contrast `expo-sqlite`'s plugin is deliberately **not** registered: with no props it writes no Gradle property and is a genuine no-op (its options are SQLCipher, FTS, libSQL, and custom build flags, none of which T4 §6 asks for), and a registered plugin that does nothing is a claim that something is configured.
- 2026-08-02 — M1/P1/T4 §12: **the JS test harness needs `babel.config.js`, which the app has never had.** Metro synthesises `babel-preset-expo` when no Babel config file exists, which is why M0 shipped a building app without one; `babel-jest` does not, and the symptom is a Flow syntax error thrown from inside React Native's own `@react-native/jest-preset/jest/setup.js` — a failure that points at `node_modules` and says nothing about the missing config. Runner is `jest-expo/android` alone rather than the multi-platform preset, since the ios/web presets would re-run every test against platform code this project does not have. One first test, a harness smoke test over the generated asset registry; it deliberately does **not** re-implement T4 §9.2's three checks, which `scripts/gen-assets.ts` already enforces by failing the build. ↯ Found while writing it: `tsconfig.json`'s `@/assets/*` → `assets/*` mapping **shadows `src/assets/`**, because the binary asset directory and the generated-module directory share a name and the more specific alias wins. `src/assets/*.generated.ts` is therefore reachable only by relative import until the alias is renamed. Left as-is and flagged rather than renamed mid-packet — it costs nothing today and the fix belongs with the first screen that imports a sprite (P8).
- 2026-08-03 — M1/P2/T6 §10.3 **deviation: the scheduled task triggers on daily + at-logon, not daily + at-startup.** §10.3 asks for an at-startup trigger with catch-up semantics; that cannot work on this host, because Docker Desktop is a user-session application and at true machine startup there is no engine to dump from. An at-startup task would fire on every boot, spend its full wait on a database that cannot exist yet, and exit non-zero — training the one signal this job has (`LastTaskResult`) to be ignored. At-logon is the honest proxy for "the machine is awake and Docker is reachable" and preserves exactly the property §10.3 wanted. `LogonType Interactive` for the same reason: a task set to "run whether the user is logged on or not" looks more robust and would fail every time. `StartWhenAvailable` is the run-if-missed half, and `backup.sh`'s own once-a-day guard is what keeps the two triggers from spending three of the seven daily slots on a day that begins with a reboot.
- 2026-08-02 — M1/P2/T6 §10.2: **the off-machine target is Google Drive, and `C:\Users\matty\OneDrive` must not be used.** That folder exists and looks like a working sync root, but OneDrive has never been signed into on this machine — no process, no `HKCU:\Software\Microsoft\OneDrive\Accounts` key, and nothing in it but the placeholder a fresh install drops. Targeting it would have produced a backup set that appears complete and contains no off-machine copy at all, which is precisely the failure §10.1 exists to prevent. Because a signed-out or unmounted sync client is invisible to a plain file write, `backup.sh` probes `BACKUP_REMOTE_DIR` with a real file on every run rather than trusting the path, and exits **2** — a distinct code, not a warning — when the local dump succeeded and the off-machine copy did not. Destination is `G:\My Drive\Development\Projects\Apps\Traverser\Backups`; both paths are `.env` keys with no defaults, so the job cannot silently fall back to writing somewhere nobody looks.
- 2026-08-02 — M1/P2/T6 §10.4, **retention keeps the N most recent distinct days/weeks/months that *have* a dump, rather than everything inside a wall-clock window.** The distinction only shows up on a machine that is off for a fortnight, which is this machine (§1.1): a wall-clock rule would prune six of the seven dailies while nobody was watching, leaving the recovery window §10.4 asks for existing only on paper. Verified against a synthetic 18-month history (539 files → 19: seven consecutive dailies, weeklies covering the following fortnight, then month-ends back twelve months) and against a gap-riddled five-month history, where only within-month duplicates were dropped. **19 rather than 23 is correct, not a shortfall** — one dump satisfies several rules at once, which is the same dedupe restic and borg apply to identical policy flags. Measured sizing, same date: a dump is 83 KB with seeded content and no player data, and a synthetic year of play (10,950 `sync_delta` rows at a pessimistic 30/day, plus 365 `activity_day` rows) adds 614 KB — so the full set in both locations is tens of megabytes for years. `-Z9` was measured at 0.5% smaller than default and rejected; the archive is mostly high-entropy UUIDs and does not compress at any level.
- 2026-08-02 — M1/P2/T6 §11.2, **`backup.sh` is POSIX sh with no GNU-only dependencies, and the date arithmetic is hand-rolled to keep it that way.** §11.2 names the Task Scheduler registration as the single sanctioned Windows-specific exception, so everything else had to survive the move to a Linux host untouched. The one thing that would have broken it was `date -d`, a GNU extension needed to bucket dumps into weeks; replaced with Howard Hinnant's `days_from_civil` in shell arithmetic, cross-checked against GNU `date` on both leap-day boundaries. Leading zeros are stripped with `${m#0}` rather than bash's `10#`, which dash does not support. Two further traps found while writing it: `.env` is **parsed, not sourced** — sourcing runs the file as shell, so a `$` or backtick in the generated `POSTGRES_PASSWORD` would become a command substitution, and a CRLF line ending would otherwise ride inside every value and turn `pg_dump -U traverser\r` into what reads as a wrong password; and under `set -e` a bare `[ … ] && cmd` list whose test is false returns non-zero and kills the script, which had silently broken the exit-2 path until it was rewritten as `if`. The dump is written to a `.partial` name the retention pass ignores and only renamed into place after `pg_restore -l` reads its table of contents back, so a truncated archive can never look like a backup.
- 2026-08-03 — M1/P2, **`register-backup-task.ps1` is saved as UTF-8 *with* BOM and keeps its string literals ASCII.** Windows PowerShell 5.1 decodes a `.ps1` as ANSI unless a BOM says otherwise, and an em dash decoded as CP1252 ends in `0x94` — a smart closing quote, which terminates the string early and throws a parser error pointing at an unrelated token several words later. The BOM is the actual fix; ASCII-only literals are the belt-and-braces half, so the script still parses if an editor ever strips it. Comments keep the project's usual `↯`/`§` markers. Worth recording because every other text file in this repo is read as UTF-8 by its toolchain and this is the one place that assumption is false.
- 2026-08-03 — M1/P3/T2 §3, **registration re-mints the bearer token rather than returning the original, because the original is unrecoverable by construction.** §3 says re-registering "returns the existing profile **and token**"; tech-01's `auth_token` stores only the SHA-256, so there is no way to return a token that was already issued. A fresh one is minted on every call and the previous rows are left live — which is what `auth_token`'s own note already anticipates when it allows several live rows per player for the reinstall case. The profile itself is *not* updated on the replay: a second registration carrying a different `traverser_name` or `timezone` reads rather than writes, because a retry path that silently renames the profile is a data-loss path. 201 on the genuine create, 200 on the replay — nothing branches on it, but it is the difference between "your first attempt failed" and "your first attempt worked and you missed the answer". Concurrency is handled by catching Postgres `23505` and re-reading rather than by a pre-check, since the pre-check has the same race it is trying to close.
- 2026-08-03 — M1/P3/T2 §1.4, ↯ **the bearer token is an `AuthenticationHandler`, not the `IEndpointFilter` the plan's wording implies — an endpoint filter cannot work here.** Minimal-API **parameter binding runs before endpoint filters**, so a filter that resolves the player has no way to publish it to a bound parameter: `CurrentPlayer.BindAsync` executes first and finds nothing. Found by running it, not by reading — the filter version returned a correct 401 for anonymous requests and a 500 for authenticated ones, which looks like a handler bug rather than a pipeline-ordering one. Authentication middleware runs before endpoint execution entirely, which is the ordering this needs, and it also brings `RequireAuthorization()` as the group marker. Authentication is a property of the **route group**, not of individual endpoints: `open` (registration only) and `secured` (everything else), so adding an endpoint cannot forget to authenticate and adding an anonymous one is a visible act. 401 bodies are written by hand in `HandleChallengeAsync` because the default challenge is an empty response with a `WWW-Authenticate` header, and §2 requires RFC 9457 with a `code` member. `revoked_at` deliberately reports the same `invalid_bearer_token` as an unknown token — tech-01 wants that distinction available *in the table*, and spelling it out in the response would confirm to a caller that a given token once existed.
- 2026-08-03 — M1/P3/T2 §2, **`AddProblemDetails` + `UseExceptionHandler` silently turned every malformed request body into a 500.** Minimal APIs signal a broken body by throwing `BadHttpRequestException`, which carries its own 400 — but registering an exception handler at all takes that translation over, and its default answer for any exception is 500. So the change made to *get* RFC 9457 shapes is the change that started reporting client bugs as server failures, which is how someone ends up reading server logs for a typo in a payload. Fixed with `ExceptionHandlerOptions.StatusCodeSelector`. Found by posting `{not json`; there is now a regression test for it, because nothing else about the app looks wrong when this is broken.
- 2026-08-03 — M1/P3, **API tests run against a disposable Postgres container (`Testcontainers.PostgreSql`, free OSS, no account), not against the dev database.** New test dependencies: `Microsoft.AspNetCore.Mvc.Testing` and `Testcontainers.PostgreSql` 4.13.0 — both free, and Docker was already a prerequisite for running the stack at all, so no new machine-level requirement. The reason is tech-06 §10.7: from M1 the dev volume holds real, unreproducible fitness history, and a suite that creates and drops databases beside it is one typo from being the thing the backup job exists to recover from. Pinned to the same `postgres:18-alpine` as `docker-compose.yml`, since testing against a different major would make the suite's agreement with production a coincidence. ↯ Two traps worth recording: (1) the container must be migrated through a **standalone `DbContext`**, because touching `WebApplicationFactory.Services` is what starts the host and §5.2's boot assertion then fires against a virgin database, killing every test in fixture setup with a message about schema drift; (2) a `Traverser.Tests.Api` namespace **shadows `Traverser.Api`** for every file in the test project, breaking `SeedIntegrityTests`'s existing relative `Api.Data.Entities.Zone` reference — C# binds the first identifier by walking outward from the current namespace, so the nearer one wins and the compiler blames the innocent file. The test namespace is `Traverser.Tests.Endpoints` for that reason alone.
- 2026-08-03 — M1/P3/T2 §2, **the two volume fields ship as decimal *strings*, not JSON numbers.** §2 says "integers or decimal strings, never floats" and `music_volume`/`sfx_volume` are the only non-integers on M1's wire; sending them as JSON numbers would hand them to JavaScript's binary float parser for no benefit. Formatted `0.00` with invariant culture, because a comma decimal separator would be a wire break. Two related snapshot decisions in the same shape: `xp_to_next` is **null at Level 60** rather than 0 or absent (the schema's way of saying accrual stops with nothing banked, GDD 1 §4 — and the client renders MAX off it), and `birth_year` starts **null meaning not-yet-collected** rather than defaulted, because a default age would silently start charging HR tier minutes against thresholds nobody chose (T3 §1.4). `vigor_current` is returned raw with no regen settlement: settling needs max Vigor, which needs gear, which is M3 — and M1 has no Vigor display and no battles to spend it on.
- 2026-08-03 — M1/P4/T2 §4 step 3, **the `activity_day` merge is an EF read-modify-write under the row lock, not the section's literal `ON CONFLICT DO UPDATE … + EXCLUDED`.** The normative rule is *always add, never assign*, and both forms satisfy it; what makes the readable form safe is the `SELECT … FROM player WHERE id = $1 FOR UPDATE` the transaction already opens with, which serialises two overlapping syncs from a retrying client. Recorded because the safety is non-local: remove or weaken that lock and the SQL upsert becomes mandatory again. Step 1 by contrast is kept as the spec's literal statement — `INSERT … ON CONFLICT (player_id, client_delta_id) DO NOTHING RETURNING` — because it *is* the idempotency mechanism, and expressing it as a pre-check for existing IDs would reintroduce exactly the race it closes. Deltas are sent as parallel arrays through `unnest` so a batch costs one round trip whatever its size; T2 §5 caps the client queue at 5,000 entries and a statement per delta would turn a long offline stretch into a five-thousand-round-trip sync.
- 2026-08-03 — M1/P4, **`activity_day.goal_met` is computed during the rollup, which T2 §4 step 3's SQL does not mention.** It is a pure function of two columns in the same row (`steps >= step_goal_snapshot`) and the M1 activity log reads it, so leaving it permanently false would be storing a wrong answer rather than deferring one. This is **not** step 9: `streak_credit_method` stays null through M1 and the streak evaluation it feeds is GDD 11's, at M4. Recomputing on every merge is safe because `step_goal_snapshot` is frozen on insert — raising the goal cannot retroactively un-hit a day that was legitimately earned (tech-01 §4), and there is a test that mutates exactly that.
- 2026-08-03 — M1/P4/T2 §4, **the sync request carries deltas and `content_version` only; `local_date` and `timezone` are deliberately omitted until something reads them.** §4's request list also names completed battles, `hr_session` upserts and queued Explore requests — all step 2 and step 8, which arrive with the battle engine at M2 — plus the client's local date and timezone, whose only consumers are step 9's grace-window lookback (M4) and step 8's rolls (M2). `content_version` **is** accepted and echoed but not enforced: M1 decision §3.2 defers the content bundle to M2, so nothing this transaction touches depends on which content the client holds, and `content_version_stale` becomes reachable when the bundle does. A field the server accepts and ignores is a promise it is not keeping, so the two unused ones are absent rather than reserved.
- 2026-08-03 — M1/P4, **a rejected allocation does not burn its `operation_id`.** Validation runs before the `client_operation` insert, so a request rejected for `insufficient_stat_points` leaves the ledger untouched and the client's corrected retry — which reuses the same ID, because T2 §2 says an operation ID is minted once and never regenerated — applies normally. Inserting the ledger row first would make the corrected retry look like a replay and silently do nothing, which is the worse failure: the player taps Confirm, sees success, and has allocated nothing. `insufficient_stat_points` is its own code rather than `validation_failed` because the request is well-formed and the client can act on it specifically (refetch the profile; the level-up it assumed did not happen).
- 2026-08-03 — M1/P4, ↯ **the replay test T2 §8 names is necessary but not sufficient, and the gap is worth knowing.** A byte-identical replay accepts zero deltas, so the transaction short-circuits before the merge runs — the test proves the short-circuit, not the "everything downstream is computed from the returned rows, never from the request" rule that produces it. Found by mutation: swapping the merge to read the request instead of the `RETURNING` set leaves the replay test green and fails only the mixed-batch test, where one delta is new and one is a replay. Both tests are kept and cross-referenced in their comments so neither is later deleted as redundant. Five mutations were run against this packet in total — per-delta Tier 3 cap, request-instead-of-returned, XP banked past 60, `activity_day` assigned instead of accumulated, and `step_goal_snapshot` refreshed on merge — and each is caught by a named test.
- 2026-08-03 — M1/P5/T4 §6, **the storage layer takes a narrow `SqliteDatabase` port rather than importing `expo-sqlite` directly, so the schema and the queue are testable off-device.** `expo-sqlite` is a native module: under Jest it resolves to a mock with no SQL engine behind it, so every assertion about a migration, a FIFO drain or a watermark would have been checking a stub's return value. Production binds the port to `expo-sqlite` (`open.ts` is the only file that imports it); tests bind it to **`node:sqlite`**, which Node 24 ships built in — a real SQLite, no new dependency, and the SQL under test is the SQL that ships. The seam is deliberately five methods and no query builder; anything richer would have behaviour of its own and the tests would start exercising the seam instead of the schema. ↯ What this does **not** cover, and must not be claimed: the native binding, `open.ts`'s pragmas, and Android's handling of the file. Those are proven on the device at P9.
- 2026-08-03 — M1/P5, ↯ **`expo-sqlite`'s `withTransactionSync` cannot be used, because SQLite has no nested transactions and this design requires nesting.** It issues a bare `BEGIN`, so one transactional function calling another throws `cannot start a transaction within a transaction` — and that is the *required* arrangement, not an exotic one: tech-03 §8.4 makes `commitReadCycle` wrap an atomic delta enqueue plus the watermark advance in one unit of work, and tech-02 §5 makes the enqueue itself atomic. Replaced with `transact()`, which opens a real transaction at depth 0 and a `SAVEPOINT` below it, tracking depth per database instance in a `WeakMap`. **Found by the P5 test suite, not by reading** — the un-nested version would have thrown on every health read cycle on the device, which is to say on the app's main loop. `withTransactionSync` is therefore absent from the port entirely, so there is no second way to open a transaction and no way to get the nesting wrong at a call site.
- 2026-08-03 — M1/P5/T4 §6.2, **schema notes worth carrying forward.** (1) `player` is keyed by a `one_row INTEGER PRIMARY KEY CHECK (one_row = 1)` with `id` merely UNIQUE — on the server `player` is keyed by uuid because it will one day hold many, but on a device there is exactly one forever (T4 §6.5), and a second row would be a silently split mirror rather than a visible error. (2) `outbox` is indexed `(created_at, client_op_id)` and the tie-break is load-bearing: tech-03 §8.1's marks mint one delta per date and per tier from a single read, all stamped from the same clock read, so same-millisecond entries are the norm and a drain order that is not total cannot be resumed deterministically after a crash. (3) Volumes are stored as `TEXT`, matching the wire — SQLite has no exact decimal type and `REAL` would be the float tech-02 §2 spends a paragraph avoiding. (4) `player.xp_to_next` is mirrored from the server snapshot rather than derived, because M1 ships no content bundle and the device has no curve to look it up in.
- 2026-08-03 — M1/P5, ↯ **a test can mask the bug it is meant to catch, and one here did.** The tech-03 §8.4 ordering test wrapped `commitReadCycle` in an outer transaction and threw afterwards — so any write that escaped the *inner* transaction was still rolled back by the outer one, and moving `setReadWatermark` outside the cycle's transaction passed all 49 tests. Replaced with a structural assertion that records the SQL a cycle issues and requires the outbox insert and the watermark insert to fall between one `BEGIN` and one `COMMIT`. Both mutation directions now fail it, and both are real: advancing **before** the deltas are durable leaves a watermark past a walk that was never queued (silent loss, app looks healthy); advancing **after** the commit means a crash in between re-reads a window whose deltas are already queued, and since P6 mints a fresh UUIDv7 per delta the re-read produces *different* ids that the server's idempotency ledger cannot recognise — so the day credits twice. Four other mutations were run and each is caught by a named test: a droppable high-water `MAX` guard, `user_version` bumped outside the migration transaction, and the two above.
- 2026-08-03 — M1/P6/T3 §4.1, ↯ **the read window is snapped back to local midnight, which §4.1 does not say and without which the high-water scheme loses activity.** §4.1 gives the window as `[max(watermark, now − 72h), now]` and §8.1 mints `observed_total(date) − reported_high_water(date)` — but that subtraction is only correct when the observed value is the day's *whole* total. A watermark at 18:00 yesterday makes the first daily bucket cover 18:00→midnight alone; that partial total sits **below** the mark already recorded for the day, the delta is zero, and the evening's steps are dropped. Permanently, because §8.1 also forbids lowering the mark, and silently, because a zero delta is exactly what a genuinely-unchanged day produces. The same argument applies to the per-`(date, tier)` HR marks. Widening the start to local midnight costs nothing — §4.1 already calls re-reading reported days "normal and expected" and §8 is what makes it safe — so the window floor is `startOfLocalDay(max(watermark, now − 72h))`. The watermark still bounds how far back a pass reads; it just can no longer open part-way through a day.
- 2026-08-03 — M1/P6/T2 §5, **`client_delta_id` draws its randomness from SQLite's `randomblob()`, because this runtime has no CSPRNG and the alternatives cost a rebuild or the guarantee.** React Native 0.86 ships no `crypto.getRandomValues`, Expo SDK 57's runtime polyfills do not add one, and `uuid` is present only as a transitive dependency (and needs a polyfill of its own). `expo-crypto` is the conventional answer and is free, but it is a native module, so installing it mid-milestone would cross tech-04 §3.2's rebuild boundary for one function. `randomblob()` is seeded from the OS entropy pool — it is the source SQLite uses internally — and reaches the app through a handle the minting code is already writing to inside the same transaction. `Math.random` was the other no-dependency option and was rejected on principle: Hermes' implementation is a non-cryptographic PRNG, and "collisions are unlikely" is not a property to put under the one key that stops a day being double-credited or a delta being dropped. The 48-bit timestamp, version and variant nibbles are forced per RFC 9562 so the id parses identically as a `Guid` and a Postgres `uuid`. **Decision taken with Matthew** rather than assumed, since one branch of it was a device rebuild.
- 2026-08-03 — M1/P6, **the outbox payload is the camelCase *domain* shape, not the wire shape.** tech-04 §8.1 puts the single `snake_case` ↔ `camelCase` boundary in P7's `dto.ts` and says nowhere else in the app sees a wire key, so what P6 serialises into `outbox.payload` is `{ clientDeltaId, activityDate, source, stepsDelta, minutesDelta, hrTier, recordedAt }` and P7 maps it at send time. Worth recording because the queue is durable: entries written by this build are read back by a later one, so the payload shape is a small persisted format and not merely an internal one. Two smaller shape decisions inside it: `hrTier` is `null` rather than absent on a step delta, matching the server's `int?`; and `recordedAt` is the read instant for every delta in a pass, which leaves within-day ordering to `client_delta_id` — safe because only Tier 3 deltas advance the server's cumulative counter and they accumulate additively regardless of order.
- 2026-08-03 — M1/P6, **fixtures §11.3's boundary row carries an arithmetically impossible span, and the code follows the property rather than the number.** The row reads "26 min span, 25 tier-1 minutes" for a timeline with an interior gap of exactly 10 minutes; 25 tiered minutes either side of a 10-minute gap span 35, and a 26-minute span would hold 16 tiered minutes. The two numbers cannot describe one timeline. Everything else in §11 verified exactly — including all three epoch-derived session ids — so this reads as a prose slip in a row whose *stated purpose* is unambiguous and is what the test asserts: GDD 11 §8.1 closes a session only after **more than** 10 consecutive sub-Tier-1 minutes, so 10 does not close and 11 does. The fixture is **not** edited (CLAUDE.md); flagged for Matthew to correct or dismiss. Eleven mutations were run against this packet and each is caught by a named test — `ceil`→`round` on the tier bounds, `>`→`>=` on the gap, mean→max BPM per minute, the Tier 3 cap applied client-side, the day total shipped instead of the increment, the later session id surviving a merge, an unfrozen session start, the ledger writes escaping the pass transaction, a single unpaginated read, permissions read by array index, and the window not snapped to local midnight.
- 2026-08-03 — M1/P7/T4 §8.4, **migration 2 adds `player.provisional`, and it is a second migration rather than an edit to the first.** tech-04 §8.4 requires projected values to be stored in the mirror flagged provisional and the flag cleared by a replacing sync response; V1 shipped without the column. Appending V2 rather than editing V1 is §6.3's rule 1 and this is the first time it cost anything — a device that already applied V1 would never see the edit, and the two databases would diverge silently with no error anywhere. One flag rather than one per column, because the projection is written as a set and replaced as a set; a half-provisional row is not a state this design has. There is now a test for the **incremental** upgrade path (a V1 database with rows in it, migrated in place), which the all-at-once migration test never exercised.
- 2026-08-03 — M1/P7/T2 §6.1, ↯ **the mirror mirrors; it does not merge — and the word "delta" makes that easy to get backwards.** §6.1's additive rule (`col = col + delta`, never last-write-wins) is the **server's** discipline, and it exists so nothing can erase real effort. What comes back on the wire is the *result* of that merge — an absolute authoritative total — and the client stores it verbatim. Adding a response to what the mirror already holds would double every sync. Caught deliberately by a mutation (`lifetime_steps = player.lifetime_steps + excluded.lifetime_steps`) that two tests fail. The same distinction governs `activity_day`, which is replaced per date rather than accumulated.
- 2026-08-03 — M1/P7/T2 §5, **the projection duplicates GDD 1 §2's XP *rates* and nothing else — not the curve, not the level-up, and not the Tier 3 daily cap.** The curve is the server's (T2 §1.1), so XP is clamped at `xp_to_next` rather than crossing it: the bar fills and stops, and the response supplies both the new level and the carried remainder. tech-04 §8.4 calls a Reveal Card for a level-up that then un-happens the worst artefact this design can produce. At Level 60 the projection awards **nothing at all** — `xp_to_next` is null there, accrual has stopped with nothing banked (GDD 1 §4) — while steps keep accruing, because Leagues and the Waymarker run long past 60. Tier 3's 20-minute cap is deliberately not applied either: it is evaluated server-side against the day's post-merge cumulative, which the device cannot know, so a hard Peak day projects slightly high and settles quietly. Guessing the cap would replace an over-projection that corrects with a wrong number that looks authoritative. `xp_lifetime` is the one value projected uncapped, since it is cumulative and needs no curve to be right.
- 2026-08-03 — M1/P7/T4 §8.1, **the pass drains one outbox kind at a time, and M1 drains only `sync_delta`.** The outbox is deliberately one table for every write kind (§6.2) — same durability, same ordering, same retry rules — but the kinds do **not** share an endpoint: `/sync` accepts deltas, allocations go to `POST /players/me/allocations`, settings to `PATCH /players/me/settings`. A drain that read the queue whole would post a mixed batch to a server that understands part of it, and because FIFO is by `created_at` the *older* progression write sorts first, so it is exactly the entry a naive drain reaches for. `peek` takes an optional kind for this reason. The progression writes themselves arrive with the screens at P8, where their replay belongs; there is a test now that enqueues an `allocation` and asserts it is neither sent nor acknowledged, so they cannot be swept into the wrong request when they land.
- 2026-08-03 — M1/P7/T2 §5, **two pieces of §5 are deliberately not built at P7: the backoff schedule and the 5,000-entry coalescing cap.** §5 asks for capped exponential backoff (1s → 60s, full jitter) and for coalescing the oldest same-`(activity_date, source)` entries once the queue reaches 5,000. Neither is in tech-04 §8.1's thirteen steps, and neither has a trigger in M1: sync fires **only** on app open and foreground (T3 §1.5), so the retry cadence is the player's own app-switching rather than a loop that could hammer a dead host, and at roughly four deltas a day the 5,000 cap is several years of uninterrupted offline play. `attempts` is still incremented on every failed upload — P5 built `recordAttempt` for this — so the diagnostic half exists even though nothing reads it to make a decision yet. Both land with the trigger that makes them matter; recorded here so they are not mistaken for oversights.
- 2026-08-03 — M1/P7, **`npx expo lint` bootstraps ESLint on first run and rewrites the lockfile; the change was reverted.** The template's `lint` script has been in `package.json` since M0 but eslint was never installed, so invoking it installed `eslint@^9` and `eslint-config-expo`, wrote `eslint.config.js`, and churned 8,000 lines of `package-lock.json` — then failed on the same run with `Cannot find module 'eslint'`. It works on the second invocation. Reverted rather than kept: adopting a linter is a toolchain decision that belongs to a packet that means to make it, not a side effect of a verification step, and the lockfile churn touched far more than the two new packages. The one real finding while it was installed (a duplicated import in a test) was fixed. **If linting is wanted, install it deliberately** — the script exists and the config it writes is the standard flat config.
- 2026-08-04 — M1/P8/T4 §12, ↯ **React Native Testing Library v14 made `render` and `fireEvent` async, and the failure mode is silent.** P1 installed RNTL but M1 wrote no component test until now, so this was never exercised. Called the v12/v13 way, `render` returns a **promise**, the tree never mounts, and every query then throws "`render` function has not been called" — which reads as a broken harness rather than a missing `await`, and sent the first diagnosis toward jest-expo presets, module duplication and the `test-renderer` peer dependency (all fine). Found by printing the constructor of what `render` returned. Both are awaited now, in both component suites, with the reason written at the top of each. Second finding in the same pass: **`accessibilityRole` does not imply `accessible`** — a `View` defaults to `accessible={false}`, so a role on one is decorative, invisible to TalkBack, and unfindable by `getByRole`. The XP bar's track carries `accessible` for that reason and not for the test's benefit.
- 2026-08-04 — M1/P8/T4 §4.1 vs §4.3, **the Character tab is one route, not `index.tsx` + `stats.tsx`.** §4.1's tree lists both files while its own annotation says the sub-tab switcher is "NOT a nested navigator — see §4.3", and §4.3 then specifies Avatar/Stats as a segmented control over always-mounted views. Those cannot both be built. §4.3 wins because it is the reasoned one and because expo-router cannot express it the other way: a `_layout.tsx` must render a navigator, so honouring the file list would force the nested navigator §4.3 rejects. `(tabs)/character.tsx` therefore hosts both panes, hiding the inactive one with `display: none` so neither unmounts and the activity log keeps its scroll position. Map and Inventory ship as stubs so the 3-tab bar is built once here rather than retrofitted at M3. **`app/` holds routes and no logic** (§13) — every screen composes from `src/ui` and reads through `src/runtime`, which is the composition root the routes are kept ignorant of.
- 2026-08-04 — M1/P8/T2 §3, **queued progression writes replay *before* the sync, and a 4xx drops the entry.** Ordering first: step 12 overwrites the mirror with the sync response's authoritative player block, so replaying an allocation *after* that block was computed means the response does not contain it — the mirror is rewritten without the points the player just spent and the allocation visibly reverts until the next pass. The replay therefore runs between steps 10 and 11. Retry policy second: this is the only place in the app where an entry is dropped without the server naming it in `accepted`/`duplicate`. A **4xx is terminal** — the server has stated an opinion and no retry changes it, so leaving the entry queued would retry it on every foreground forever and block everything behind it — while a 5xx or an unreachable host stays queued. A dropped write is reported to Sentry rather than swallowed. `peek` takes an optional `kind` so the drain never posts a mixed batch to an endpoint that understands part of it.
- 2026-08-04 — M1/P8/T6 §13.1, **registration persists in a specific order, and each step guards the one before it.** (1) The client-minted `player_id` is written to secure storage **before** the request: a crash between the server inserting the row and the token arriving would otherwise orphan a profile nobody can claim, and minting a fresh id on retry would do it again on every attempt — reusing the stored id is what makes `POST /players`'s idempotency worth having. (2) The token is saved **before** the mirror: an existing player row *is* the registered/not-registered signal the boot router reads, so a mirror written without a credential launches the app straight into the tabs holding no token, 401ing forever, with onboarding unreachable. An empty mirror is recoverable; that state is not. Both orderings were mutation-tested, and (2) initially survived — the test that now kills it makes secure storage refuse the write. (3) The birth year goes through the ordinary queued-write path because `POST /players` does not accept it (T3 §1.4's deviation lands on `PATCH /settings`). The restore branch mirrors this: `GET /players/me` proves the credentials *before* anything is stored, and a token whose profile id disagrees with the pasted one is rejected outright.
- 2026-08-04 — M1/P8/T6 §10.5, **the identity export uses React Native's own `Share` plus selectable text — no new dependency and no rebuild.** §13.1 says "displays or exports", and both are provided: *Export identity* shares a JSON document anywhere Android can send it, *Show it instead* renders it as `selectable` text to copy by hand. `expo-file-system`/`expo-sharing`/`expo-clipboard` were all avoidable, and each would have crossed tech-04 §3.2's rebuild boundary for an affordance the core `Share` module already provides. This closes the obligation outstanding since Phase 1 close-out and makes the backup set four members; the file itself joins the Drive folder at P9, since it can only be produced on the device. README's Backups section now documents producing it, and the re-export-after-re-registration trap.
- 2026-08-04 — M1/P8, ⟨open⟩ **the app's visual style has no owner in the spec set, and P8 invented one.** Raised by Matthew after P8 landed. GDD 13 §10 hands off an *"Art phase (future, separate Claude Project)"*, but its scope is assets: the tier-colour palette for gear borders, the type-effectiveness iconography, the layered-gear sprite pipeline, and the sprite/audio files T4 §9.3's placeholder system is built to swap in. GDD 13 says outright that it gives *"structural requirements rather than art direction"* — it fixes what is on each screen and in what order, never what any of it looks like. **Nothing covers the app's own skin**: background and surface colours, typography, spacing rhythm, control styling. There is no font decision anywhere either; the app renders in Android's system Roboto and `expo-font` is installed and unused. The art phase also has no milestone — M5 owns audio and notifications with an M-number, while this is "future". So P8 needed something to render and I wrote `app/src/ui/theme.ts` — a fixed dark palette (near-black ground, bronze accent, parchment text), six spacing steps, six type steps. **That is unratified taste, not a spec, and it is now behind all ten screens.** Recorded so it cannot pass for a decision that was made. Deliberately deferred to P9 rather than resolved now: a design language is premature while half the screens do not exist and there is no art, and P9 is the first time it will be *seen* rather than read. Matthew agreed to defer conditional on the theme staying cheap to change, so that was verified rather than asserted — **zero hardcoded colours outside `theme.ts`** across the whole app, and the three remaining literals (two border radii, one control height) were routed through tokens in the same pass. `MIN_TOUCH_TARGET` is deliberately *not* a theme token: 44dp is an accessibility floor a restyle must not be able to shrink.
- 2026-08-05 — M1/P2 §10.3, ↯ **the backup task runs windowless via `git-bash.exe`, and the `>/dev/null 2>&1` that makes that work is load-bearing.** Raised by Matthew: the at-logon trigger put a Git Bash console on screen at every boot, sitting on "waiting for the database" for as long as Docker Desktop took to come up. Switching the action from `bin\bash.exe` (console subsystem — Task Scheduler always gives it a window) to `git-bash.exe --no-needs-console --hide` hides it while still blocking on bash and returning its exit code, so `LastTaskResult`'s 0/1/2 contract survives; both were verified rather than assumed. **Windowless means the process has no stdout**, and `backup.sh`'s `log()` ends in `| tee -a "$LOG_FILE"` — tee's write to a nonexistent fd 1 fails, `set -e` sees the failed pipeline, and the job died at its *first log line* with exit 1 and nothing in backup.log. Redirecting inside the launcher fixes it at the right layer: `backup.sh` stays portable and keeps tee for the by-hand path, where stdout is the point. The at-logon trigger also gained a 3-minute delay. **There is deliberately no "when Docker Desktop starts" trigger** — Docker Desktop is a per-user AppData install with no Windows service and no event log provider, so Task Scheduler has nothing to subscribe to, and the near-miss proxies are worse than they look (WSLService is Automatic and starts at boot regardless; vmcompute starts for any WSL session). The delay plus `backup.sh`'s `pg_isready` loop *is* the "start when Docker is up" mechanism, and it waits on the stricter condition: the engine's window being open does not mean the database answers.
- 2026-08-05 — M1/P2 §10 vs §9.4, ↯ **a hidden job needs a way to fail loudly, so the task now toasts on a non-zero exit — gated on Docker Desktop actually running.** The two halves are one decision: hiding the console (entry above) removed the only signal that anything had happened, and the Aug 4 and Aug 5 runs had *already* failed their off-machine copy unnoticed with the window still visible, so silence was the pre-existing bug rather than a cost of hiding it. `notify-backup-failure.ps1` raises a WinRT toast — non-modal so it cannot steal focus out of a game, and it persists in Action Center, which is the actual requirement: a 6pm failure has to still be readable at 11pm. **This is not §9.4's "no alerting".** §9.4 and the §1.1 assumptions table reject *uptime monitoring* — pages about a host that is powered off by intention, which "would fire nightly and mean nothing". The gate is what keeps this on the right side of that line: Matthew boots plenty of evenings to game rather than to work on Traverser, and the database cannot change while the engine is off, so on those boots a skipped backup is the **correct** outcome and the run stands down in ~0.3s saying nothing. Ungated, a toast would fire on every gaming night and be trained away inside a week — §1.1's objection, earned. ↯ **The gate tests for Docker Desktop's *presence*, not for the database answering, and those differ by about ten minutes**: the Aug 4/Aug 5 logs show 8–10 minutes from task start to a responsive Postgres, so the "shorten the wait to ~180s" instinct — which is where this started — would have failed both of those genuine backup nights while looking like a tidy-up. Presence answers "did Matthew mean to run the stack today"; `backup.sh`'s unchanged 600s `pg_isready` loop then absorbs however slow the cold start is. `backup.sh` is untouched by all of this and its 0/1/2 exit codes are unchanged — the notifier is scheduling furniture, second Windows-specific file in the path, and §11.2's exception is worded for one, so: it contains no backup logic, and a Linux host deletes both `.ps1` files for a cron line. **The gap this opened, closed the same day:** gating on presence means the backup only happens if Docker is up within roughly thirteen minutes of logon (3m delay + 600s wait), and a day where Matthew boots at 18:00 to game and starts Docker at 20:00 would silently get no backup — *more* quietly than before the gate existed. So the logon trigger repeats **hourly, indefinitely**, which is what lets one trigger see an evening rather than a moment. The two guards that were already there are what make it free: a non-Docker hour stops at the tasklist gate, and an hour after a successful dump stops at §10.3's once-a-day guard, so a no-op costs ~1.8s and never reaches Postgres. `New-ScheduledTaskTrigger` has no `-RepetitionInterval` for its `-AtLogOn` form, so the repetition is lifted off a throwaway `-Once` trigger; omitting `-RepetitionDuration` is what makes it indefinite rather than an hour long. ⟨Accepted cost: each no-op appends one "dump already exists" line to `backup.log`, so a working evening adds four or five lines that say nothing. Gaming evenings add none — the gate returns before `backup.sh` starts. At `log()`'s 1000-line trim that still leaves something like a hundred days of history, but if the file ever reads as noise, raising the trim is the fix, not removing the line.⟩
- 2026-08-05 — M1/P9/T6 §10.6, **restore drill rehearsed and passed, against seeded content rather than real data.** §10.6 asks for the drill "immediately after the first real step sync"; run early as a rehearsal because discovering a broken restore *before* there is anything to lose is cheaper still. `createdb` → `pg_restore --clean --if-exists` → spot-check → `dropdb`, all four steps clean: exit 0, and the restored copy matched the live database exactly on `xp_curve` 60, zones 4, enemies 13, enemy moves 28, gear 21, items 18, `content_version` 1. `xp_curve` level 60 came back with a **null** `xp_to_next` — checked specifically, because that null is the schema saying accrual stops with nothing banked (GDD 1 §4) and a null quietly becoming 0 is exactly what a bad round trip looks like. ⚠️ **The spec's own command does not run as written**: §10.2 uses `traverser` for both user and database, and the real values are `traverser_admin` / `traverser_db` from `infra/.env`. README now says so and tells you to grep the file rather than trust either document. ☐ The §10.6 drill against real fitness history is still owed once the phone has synced a day — a rehearsal on empty tables cannot prove that a database with history in it restores.
- 2026-08-05 — M1/P9, ↯ **the running API container was still the 2026-08-02 image: nothing from P3–P8 had ever been served.** Found by curling the tailnet URL before the device build — `/content/version` answered **200 unauthenticated** (decision §3.3 makes it authenticated) and `/players/me` and `/sync` both 404'd, because they did not exist in that image. `docker compose up -d` alone does not rebuild on source change, and every API test since P3 has run against Testcontainers, which builds from source and so never noticed. After `--build`: all three answer 401 with the RFC 9457 body and its `code` member, as designed. Worth recording as a standing trap — the API tests passing is **not** evidence that the API a phone can reach is current, and P9 is the first packet where anything but a container could tell the difference.
- 2026-08-05 — M1/P9, ↯ **`parseContentVersion` read the wrong field, and 239 green tests could not see it.** The server sends `content_version` (`ContentVersionResponse(int ContentVersion)`); the client read `version`. The wire fixture was hand-written from the C# record's *shape* rather than from a response, so the fixture and the parser agreed with each other and both disagreed with the server. On the device this throws `WireFormatError` at step 10 of **every** sync pass, which the pass maps to `rejected` — deltas stay safely queued, nothing is lost, and the symptom is "sync silently never works" with nothing pointing at the cause. Fixed, and fixed as a class rather than an instance: real responses from a throwaway `WireProbe` player are now checked in under `src/sync/__tests__/recorded/` and parsed by `recorded.test.ts`, which additionally asserts that the synthetic fixtures carry **exactly** the server's key set — a field the server has and the fixture lacks is a parser never exercised, and the reverse is a parser asserting something untrue. Verified by reverting the bug: the recorded test fails, the other 245 still pass. The probe player was deleted afterwards and the cascade checked (0 rows across `player`, `sync_delta`, `activity_day`, `auth_token`, `player_zone_progress`; `xp_curve` still 60). No token is in the recorded files. **Re-record whenever a contract changes** — the API is the authority and these files are how that authority reaches the client suite.
- 2026-08-05 — M1/P9/T2 §8, **the replay property verified against the live server, not just Testcontainers.** Sent 8,000 steps + 45 Tier-2 minutes: 400 + 225 = **625 XP**, level 1 → 4, three level-ups, 9 stat points, `lifetime_steps` 8,000, 8 Leagues, `goal_met` true — tech-02 §4's worked example reproducing exactly. Re-sent the identical payload: byte-identical player block, `level_ups` empty, `accepted_delta_ids` empty, both ids in `duplicate_delta_ids`, and `activity_days` **empty** — because step 3 rolls up only what step 1's `RETURNING` produced, so a replay has nothing to roll up. §8 says that if a future change breaks the design it breaks here; it is now checked in as a recorded fixture so it breaks in the client suite too.
- 2026-08-05 — M1/P9/T6 §9, ↯ **`app.config.ts`'s `disableAutoUpload: true` never disabled anything, and the first release build this project ever attempted is what proved it.** `@sentry/react-native`'s `sentry.gradle` decides with `System.getenv('SENTRY_DISABLE_AUTO_UPLOAD') != 'true'` and reads nothing from the Expo plugin config, so `app:assembleRelease` failed in `createBundleReleaseJsAndAssets_SentryUpload` — sentry-cli has no auth token, because M1 plan §2.2 defers source-map upload to M5. Nothing caught it for three milestones because the upload task is gated `onlyIf` on the **release** variant and every build through M0, P1 and P8 was a debug one: the config asserted a guarantee that had never once been executed. The real switch is now `SENTRY_DISABLE_AUTO_UPLOAD=true` in `app/.env`, documented at length in `.env.example` since it is the one key there that Gradle reads rather than the app; the plugin option stays set as a declaration of intent for whatever does read it, with a comment saying plainly that it is not the lever. **Decided with Matthew** between four options (env key, a local Gradle plugin, doing the upload properly now, or only correcting the comment). Found while regenerating `android/`, which was still the **Aug 2 / P1** output despite `app.config.ts` having been edited since — a reminder that tech-04 §1.1's "the config file *is* the native project" only holds if prebuild is actually re-run, and that a stale generated project can hide a config change for weeks.
- 2026-08-05 — M1/P9/T3 §2–§3, ↯ **`react-native-health-connect` never initialises its own permission delegate, and without it the app hard-crashes on GDD 10 screen 2.** Reported by Matthew from the device: title screen fine, Begin fine, **Continue** kills the process. `kotlin.UninitializedPropertyAccessException: lateinit property requestPermission has not been initialized` at `HealthConnectPermissionDelegate.launchPermissionsDialog`. The library keeps its `ActivityResultLauncher` in a `lateinit` on an `object` singleton and documents `setPermissionDelegate(this)` in `MainActivity.onCreate` as the *app's* responsibility; its bundled `app.plugin.js` does not do it — that plugin only pushes the Android-13-and-below rationale intent-filter into the manifest. So following the library's Expo instructions to the letter produces a build that compiles, installs, launches, and dies on the second screen of a first launch. Fixed with `plugins/withHealthConnectPermissionDelegate.ts`, a local config plugin patching `MainActivity.onCreate` — a hand-edit to `MainActivity.kt` would survive exactly until the next `expo prebuild --clean` (tech-04 §1.1), which is how this would come back months later looking causeless. It anchors on `super.onCreate(` rather than the expo-splashscreen generated block, which carries a `DO NOT MODIFY` marker and a content hash, and it throws with an explanatory message if the anchor ever disappears. **Nothing before P9 could have caught it**: no test reaches a native `lateinit`, `tsc` and jest do not compile Kotlin, and the 2026-07-26 spike ran against a scratch app whose MainActivity had been edited by hand — so the spike proved the *library* works while proving nothing about how this app wires it. tech-03 §2 amended in place.
- 2026-08-05 — M1/P9, **first real sync verified end to end, and every number reconciles against GDD 1 §2.** Four days read (Aug 2–5) because a fresh install has no watermark, so tech-03 §4.1 falls back to `now − 72h` and P6's local-midnight snap opens it at Aug 2 00:00 — the window behaving exactly as specified, observed rather than reasoned about for the first time. Step XP at 1 per 20 floored: 3,042→152, 11,163→558, 7,021→351, 7,437→371. Aug 5 additionally carried 140 Tier-1 and 1 Tier-2 minute = 420 + 5, giving 796 for the day. Total 1,857 = `xp_lifetime`. Against the seeded curve, cumulative to Level 6 is 1,595, so `xp_current` 262 and Level 6 with 15 unspent points (five level-ups × 3) are all exactly right; `lifetime_steps` 28,663 → 28 Leagues; `goal_met` false only on the 3,042 day. **`birth_year` arrived server-side as 1992**, which is the whole point of the check: registration does not accept it (T3 §1.4 puts it on `PATCH /settings`), so it went through P8's queued-write path and replayed on the first sync — that path had never run against the real server. `Intl` also resolved a real IANA zone on Hermes (`America/Toronto`), so `deviceTimezone()`'s `'UTC'` fallback is belt-and-braces rather than load-bearing.
- 2026-08-05 — M1/P9/T6 §10.6, **restore drill run against real fitness history and passed** — the version the spec actually asks for, as opposed to the earlier rehearsal on empty tables. A dump was forced immediately after the first sync (the nightly one predated it), restored into `traverser_restore_test`, and spot-checked per §10.6: a known `activity_day` row (Aug 3 = 11,163 steps), the player's level (6), plus `lifetime_steps` 28,663, `birth_year` 1992, 140 Tier-1 minutes, 6 `sync_delta` rows and `xp_curve` still 60. Dropped afterwards; `traverser_db` is the only database left. Off-machine copy confirmed in Drive. **§10's obligation is now discharged rather than merely built** — the backup has been proven restorable with something in it worth restoring, which is the distinction §10.6 exists to force.
- 2026-08-05 — M1/P9, ⟨balance observation, no code change⟩ **Tier 1 HR minutes out-earned step XP on an ordinary day.** At age 34 `HRmax` is 186, so GDD 1 §2.2's Tier 1 floor is `ceil(186 × 0.50)` = **93 BPM** — a threshold a brisk walk or a flight of stairs clears. Aug 5 logged 140 Tier-1 minutes against 7,437 steps: 420 XP from heart rate versus 371 from walking, on a day with no workout (only 1 Tier-2 minute). That is per spec — GDD 1 §2.2 pays 3 XP/min at Tier 1 and caps only Tier 3 — but GDD 1 §3's pacing baselines assume 400/700 XP for active/highly-active days, and this was 796 without a workout. Recorded rather than acted on: the formulas are locked and CLAUDE.md forbids changing game behaviour to suit the code. Flagged for a GDD balance pass, with the note that the effect scales with age — a younger player has a higher floor and earns less passively, which is the opposite of the intuition.
- 2026-08-05 — M1/P9/T3 §4.1, ↯ **a device's first read now establishes a baseline and credits nothing.** Raised by Matthew from the device: *"when a fresh install happens it should start recording steps from that point forward… each user should start at Level 1."* He is right, and the consequences run deeper than the level number. §4.1's 72-hour fallback exists for backfill and grace, but a fresh install has no watermark, so it also harvests whatever history Health Connect already holds — his first sync read four days back, credited 28,663 pre-existing steps and landed on **Level 6 before a step had been taken in the game**. Beyond contradicting the design intent, it breaks **GDD 10 §6's tutorial battle**, which is scripted with verified damage values against Level 1 stats and fights an enemy whose level always equals the player's; and — found while reasoning about the fix rather than reported — it **double-credits a restored identity**, because the high-water marks live in tech-04 §6.2's device-only tables and come back empty on a new phone, so the client re-mints *fresh* delta ids for days the server already holds and tech-02 §6.1's additive merge adds them a second time. That is the one hole in the delta protocol `client_delta_id` cannot close, because the ids really are new. All three are the same situation — a device that has never consumed a read has no basis for claiming history it can merely see — so one rule fixes all three, plus the permission-granted-later case, in one line of `commitHealthRead`. The marks are still raised; only the deltas are dropped, which is exactly what makes it a baseline rather than an amnesia. **14 existing tests failed on the change and every one of them was right to** — they had been getting their first-read credit by accident; they now say `pastFirstRead(db)` explicitly, and the baseline has five tests of its own. Two mutations confirm it. tech-03 §4.1 amended in place.
- 2026-08-06 — M1/P9/T3 §4.1, ↯ **the baseline is per *source*, not per device — a single flag was consumed by steps and let heart rate credit the whole history.** Reported by Matthew on the second fresh install: Level 3, 431 XP, activity showing **0 steps**. The deltas said it exactly — `hr 2026-08-05 142 min tier1` and `1 min tier2` credited in full at 02:26 UTC, while `steps 2026-08-05` was correctly baselined to a 62-step increment two minutes later. Cause: the two sources become readable at different moments. Heart rate additionally needs a birth year, and the birth year only exists from registration onward, so the *first* pass of a fresh install reads steps and not HR — consuming the device-wide baseline — and the first HR read then lands after it. Fixed by deciding the baseline per source (`readSources` now travels on `HealthSnapshot`, so "read and found nothing" is distinguishable from "did not look"), plus a zero mark written when a source is read with nothing to show — otherwise the next read baselines again, which is the same bug one day later and is reachable by registering at 6am before moving. Both halves mutation-tested; the second mutation initially survived because the first test only covered the steps branch, and the HR branch is the one that actually arrives late. **Related fix in the same report:** the app asked for a birth year that had just been entered, because the health banner reflects the *last completed pass* and registration triggered no new one — `04-name.tsx` now syncs immediately after `refreshIdentity()`, which also happens to be the pass that baselines HR. Neither bug was reachable before P9: both need a real fresh install where permission, registration and birth year land in that order.
- 2026-08-06 — M1/P9, **pull-to-refresh added to the Character screen, and three RN layout divergences learned building it.** Not in the plan; requested by Matthew after the first real walk, because sync fires only on `AppState → 'active'` (T3 §1.5) and a walk with the phone already foregrounded may not re-fire it. `syncNow` already de-duplicates, so a pull landing on an automatic pass joins it. The divergences are worth carrying into M2's screens: (1) **`display: 'none'` on a `ScrollView` still claims its `flex`** — the hidden Avatar pane left a full-height invisible block above the stats card; the wrapper that hides must be a `View`, with the scroll view inside. (2) **A `RefreshControl` needs a scroll host at the point of the gesture, not merely on screen** — the Stats pane had its control mounted (confirmed by two `ReactSwipeRefreshLayout` nodes in `dumpsys activity top`) but the top of the pane was a plain `View` holding the stat card, so a pull starting there reached nothing. Fixed by making the stat panel the list's `ListHeaderComponent`, so the whole pane is one scroll host; wrapping both in a `ScrollView` would have fixed the gesture and nested a VirtualizedList, trading a dead gesture for a broken list. (3) A `FlatList` with no `flex` sizes to content, so a near-empty log is a thin strip. ↯ Also fixed in passing: the screen re-read the mirror off `lastServerOutcome`, but two consecutive successful syncs both read `'synced'` — the value never changes, so a pull would have spun and shown stale numbers. Every path that touches the mirror now calls `reload()` directly. **None of this is reachable by the test suite** — RNTL renders a tree, not a layout, and all three were found on the device.
- 2026-08-06 — M1/P9, **the visual-style read is deferred to M2 planning, and the `⟨open⟩` from 2026-08-04 closes as deferred-with-a-home.** Matthew's call after seeing the app on the device: *"what we have now is fine and the styling isn't crucial right now."* That is the answer the P9 item asked for — "roughly right" rather than "nothing like it" — so P8's `src/ui/theme.ts` stands as the working skin for M2's screens. It remains **invented rather than specified**: nothing in the GDD or the tech specs owns background colour, typography, control styling or a font choice, and GDD 13 §10's art phase covers assets and gear-tier colours only. The deferral is cheap because it was made cheap on purpose — zero hardcoded colours outside `theme.ts`, verified at P8 — so a later restyle stays a one-file change however many screens are built against it by then. **P11 writes `traverser-m2-plan.md` and must carry the item**, either widening the art phase's scope in writing or scheduling a design-language section; P10 item 8 is amended to check for that rather than to resolve it. The thing this must not become is the failure the 08-04 entry named: reaching M5 with five milestones of screens built against a look nobody chose.
