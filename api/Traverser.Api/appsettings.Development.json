{
  "_comment": "The password is deliberately absent — it lives in user-secrets locally (dotnet user-secrets set \"ConnectionStrings:Traverser\" \"...\") and in ConnectionStrings__Traverser in the environment for Compose. Values here mirror infra/.env.",
  "ConnectionStrings": {
    "Traverser": "Host=localhost;Port=5432;Database=traverser_db;Username=traverser_admin;Password="
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
